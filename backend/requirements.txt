fastapi==0.141.1          # F-22: lifts the starlette<0.42 pin (8 advisories)
uvicorn[standard]==0.34.0
# audit #7: gunicorn process manager for multi-worker scale-out (uvicorn worker
# class). Worker count is gated by scale_preflight (Postgres-only).
gunicorn==23.0.0
asyncpg==0.30.0
pydantic==2.10.4
# SEC-AUDIT-2026-07-29: pip-audit flagged CVEs in the pinned versions below.
# Upgraded and re-verified against the full backend suite (1551 pass).
python-dotenv==1.2.2          # was 1.0.1  (PYSEC-2026-2270)
httpx==0.27.2
python-multipart==0.0.31     # was 0.0.20 (6 advisories, incl. PYSEC-2026-3040)
# Signs the facilitator auth cookie — the highest-value dependency here.
# 3.3.0 carried PYSEC-2024-232/233 (algorithm confusion / DoS). Every decode
# call already pins algorithms=["HS256"], which blunts the confusion class,
# but the library fix belongs in place rather than relying on call-site care.
PyJWT==2.13.0             # F-22: replaces python-jose, which pinned pyasn1<0.5.0
starlette==1.3.1          # F-22: explicit floor — PYSEC-2026-249 needs >=1.3.1
bcrypt==4.2.1
openpyxl==3.1.5
