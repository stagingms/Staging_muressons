# Archived manual diagnostic harnesses

Standalone e2e/diagnostic scripts retired from backend/ root (AR2b).
NOT part of the pytest suite (`backend/tests/`); most need a live server.
Still runnable from the archive:

    cd backend
    PYTHONPATH=. python ../archive/backend_manual_tests/test_deep_audit.py
