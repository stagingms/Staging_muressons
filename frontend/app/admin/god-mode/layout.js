export const metadata = {
    title: 'God mode- Muressons Simulation',
    description: 'Real-time cohort monitoring, manual overrides, and message injection for simulation facilitators.',
};

export default function GodModeLayout({ children }) {
    return children;
}
