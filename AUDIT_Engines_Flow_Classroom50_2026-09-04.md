**NO-GO** for HEAD `d3bbf09` (`fix/launch-readiness-20260903`) as it stands. It becomes **GO-WITH-CONDITIONS** when conditions 1–6 below are done; conditions 7–12 are strongly recommended but not blocking.

# AUDIT — Engines, Flow, Classroom-50 — 2026-09-04

Report for the cohort deployment of `fix/launch-readiness-20260903` @ `d3bbf09`; written to the repo root as requested. Verdict is the first line above.

**Blocking conditions** (each traceable to a finding; minutes are for the owner or facilitator, as marked):
1. **F-01** — ship the change-password modal in the cockpit (owner, ~20 min) *or* run the "Forgot your password?" drill on slide 1 and brief the recovery script (facilitator, 5 min + 2 min per stuck person). As shipped, every freshly issued participant is refused every write after login and no screen tells them why.
2. **F-04** — forward the cohort seed into `new_global` before `post_tick`/`run_new_engines` (owner, 10 min) *or* switch off "NPC stakeholders" per cohort (facilitator, 1 min, loses the regulator mechanic). Otherwise identical teams differ by up to ~$17M from an unseeded fine.
3. **F-07** — inspect and, if stale, patch `/data/simulation_config.json` on the Railway volume and confirm the deploy log has no `[CONFIG] WARNING` (owner, 10 min). On a stale volume every team is fined $1.6–3.75M in round 1.
4. **F-02, F-03, F-06, F-08** — either land the four small diffs (owner, ~30 min) or apply the projection rules in the runbook: never project the Round Debrief "Decision Made" cards, the Debrief Narrative reputation column / turning points / predicted-vs-actual, the Front Page P&L block, or the "Road Not Taken" panel (facilitator, 0 min).
5. **F-05** — set `payback_rounds` to 2 and correct the R8-C text (owner, 5 min) *or* tell the room before R8 that the desalination payback does not land inside the game (facilitator, 1 min).
6. **F-18** — after creating each cohort, open Round Timeline → Round Pacing, click **Manual**, Apply (facilitator, 2 min per cohort). The wizard's pacing choice is not enforced.

**Recommended:** 7 gate the ten unauthenticated answer-key endpoints and the forgeable analytics guard (F-10, F-11; owner, 15 min); 8 disable per cohort `biodiversity_engine_enabled`, `tcfd_scenarios_enabled`, `mirror_debrief`, `mr_ladder_reveal`, `rewind_ribbon` (F-19, F-15; facilitator, 2 min); 9 do not create `brsr_ngrbc` cohorts (VAL-06); 10 in `multi_toggles` cohorts, instruct every team to pick an option in every pillar area (FLAG-2); 11 rehearse three test players through R1–R2 on production after the deploy (facilitator, 15 min); 12 confirm the Railway volume is mounted at `/data` and point an external monitor at `/health?strict=1` (owner, 10 min).

**Reasoning (≤150 words).** The financial core reconciles to the cent inside `process_tick` on all five shipped paradigms, M_R has one arbiter at the finale, and the stochastic engine draws are cohort-seeded — that part of the model is sound. What is not classroom-ready is the seam between engine and room: a first-login gate that the cockpit never renders (the whole room is stuck at the R1 stakeholder map), one unseeded $5–25M draw that breaks fairness between identical teams, and five debrief/projection surfaces that assert numbers the engine did not produce — the wrong decision on every debrief card, reputation 0 shown as 50, counterfactuals that are a pipeline artifact, a $15M payback promised and never paid, and three different "closing treasury" values. Each has a fix of a few lines or a facilitator rule; none can be discovered from a stack trace during class.

---

## Assumptions

- Cohort ≈50 participants + 1 facilitator (possibly a co-facilitator), all live at once; executive education; paying, senior; no second attempt. Burst login (~50 in five minutes); round-synchronised play (~50 commits in a 60-s window at each of ten round boundaries).
- Session shape: audited **both** a single 2–4 h sitting **and** a split across up to five days (owner's answer). Facilitator cookie 8 h, player token 12 h, no sliding expiry for players (verified, ACC §D) — day-2+ requires re-login with the personal password; that works.
- Devices unmanaged, mixed networks (owner's answer): some share one campus egress IP; per-IP login limit is 300/min so 50 logins do not trip it (verified).
- Paradigm: any of the five in `config.py:32-34` (`legacy_abc`, `multi_toggles`, `advanced_climate`, `healthcare`, `brsr_ngrbc`); difficulty medium-to-high (`advanced`/`expert`). Probes ran mostly `legacy_abc` and `multi_toggles`, some `advanced_climate`, `healthcare`, `brsr_ngrbc` (coverage section says which).
- Deployment: Railway single image, PostgreSQL store, single worker, deploy-on-push; the owner will push this branch before class. Which branch Railway watches, the contents of the `/data` volume, and the Railway environment variables are **not verifiable from here** — each carries a settling command below.
- The facilitator is not the developer; anything needing developer intervention during class is an outage.
- Environment caveat: every executable probe ran against the in-memory store (`USE_MEMORY_DB=true`) with a scratch data dir on the owner's machine; Postgres paths are static reads marked as such. Background processes are killed between shell calls, so every probe ran in the foreground.

**Disclosures — repo state after the audit.** The tree is byte-identical to `d3bbf09` plus the pre-existing untracked files, except this report and the companion archive (below). Three side-effects occurred and were reversed: (1) my first `git status` left a zero-byte `.git/index.lock` (the shell cannot delete files by default) — removed after requesting delete permission; (2) a config probe exercised the real `/api/admin/config/upload`, which moves the uploaded sheet over `<repo>/simulation_config.xlsx` (admin_router.py:9276) — restored from HEAD, md5 identical, only the mtime changed; (3) an access-sweep probe called `PUT /api/admin/pillar-config/pharma/order` with `{}`, which re-serialised `backend/db/pillar_overrides_pharma.json` in the working tree (that endpoint writes into `backend/db/`, see ACC-6 in Appendix D) — restored from HEAD. The audit was interrupted once by a session rate limit and resumed with agent context intact.

**Companion archive.** All probe scripts, logs and the eleven full cluster reports are in `Claude outputs/audit_2026-09-04_probes_and_cluster_reports.tar.gz` (also delivered in the chat). Delete it if unwanted; nothing in the report depends on it being in the tree.

---

## Do-before-class list (ordered; each executable by the named person in the stated minutes)

| # | Who | Min | Action | Finding |
|---|---|---|---|---|
| 1 | Owner | 20 | Apply the F-01 diff (render `ChangePasswordModal` in the cockpit when `sim.mustChangePassword`), rebuild, redeploy. **If not:** facilitator puts on slide 1: "Before signing in, click **Forgot your password?**, enter your team ID, current password = `ID@123`, choose a new password (≥8 chars), then sign in." Anyone already signed in: Log out (🚪, click twice) and do the same. | F-01 |
| 2 | Owner | 10 | Apply the F-04 diff in `router.py` before `post_tick` (copies `stochastic_seed` into `new_global["active_event_flags"]`). **If not:** per cohort, switch off NPC stakeholders (`npc_stakeholders_enabled`) in cohort settings. | F-04 |
| 3 | Owner | 10 | With the Railway CLI logged into the production account: run the inspect one-liner in F-07; if any value differs from `cbam 100 imit 0.05 ratchet 20.0 synergy 0.06 ncd 5000 1000 1000`, run the patch one-liner, restart the service, re-run the inspect, and confirm the deploy log contains no `[CONFIG] WARNING`. If `/data/simulation_config.json` does not exist, do nothing — the HEAD image seeds the correct copy on first boot. | F-07 |
| 4 | Owner | 30 | Apply the diffs for F-02 (`admin_router.py:8555`), F-03 (`admin_debrief_narrative.py:46` + the 18 `\|\| 50` sites), F-06 (`router.py:3335` baseline), F-08 (re-stamp finale figures after `run_new_engines`). **If not:** facilitator follows the projection rules in the runbook. | F-02/03/06/08 |
| 5 | Owner | 5 | `round_configs.py:603` `"payback_rounds": 3` → `2`; fix the text at :588-592 and the two teleprompter strings (`admin_teleprompter.py:570`, `FacilitatorTeleprompter.js:1320`). **If not:** brief before R8. | F-05 |
| 6 | Owner | 15 | Add `_g: None = Depends(require_facilitator)` to the ten handlers in F-10; replace the header compare in `admin_analytics.py:621-624` with `_assert_player_owns_session` (F-11). | F-10, F-11 |
| 7 | Owner | 5 | Push the branch; in Railway → Service → Settings → Source confirm the watched branch is the one pushed; after deploy, `GET /api/health` → `.build` must show commit `d3bbf09` or later. | C3 |
| 8 | Owner | 5 | Railway → Service → Volumes: a volume mounted at `/data`. Then the end-to-end proof in DEPLOYMENT_CHECKLIST.md:46-48 (create a facilitator → Redeploy → still logs in). | OPS-3 |
| 9 | Facilitator | 2/cohort | After creating each cohort: Round Timeline → Round Pacing → click **Manual** → Apply. Do not rely on the wizard's pacing field. Never use "Timed" with an interval (OPS-1). | F-18 |
| 10 | Facilitator | 2/cohort | Cohort settings: turn OFF `biodiversity_engine_enabled`, `tcfd_scenarios_enabled`, `mirror_debrief`, `mr_ladder_reveal`, `rewind_ribbon`, `r6_revelation_enabled`, `r7_budget_allocation_enabled`, `r8_tribunal_enabled`. Keep the Trading-Floor switch OFF. | F-15, F-19, IMP-04 |
| 11 | Facilitator | 1 | Do not create `brsr_ngrbc` cohorts (VAL-06). In `multi_toggles` cohorts tell teams to choose an option in every pillar area, especially R5 Physical Defence and R8 water (FLAG-2). | VAL-06, FLAG-2 |
| 12 | Facilitator | 15 | Rehearsal on production after the deploy: three test players through sign-in → R1 map → R1 commit → R2 matrix; check the admin leaderboard shows all three; check `/api/admin/teleprompter` from a logged-out browser returns 401 (if item 6 was done). | F-01, F-10 |
| 13 | Facilitator | 5 | Print the runbook page (end of this document) and the projection rules. Brief: "do not reload after committing — wait for the screen to move" (F-18). | F-18 |

---

## Engine verdicts (Q1)

Accuracy = does the code compute what it claims. Relevance = is the computed thing a defensible model at magnitudes a domain-literate participant would accept. Confidence reflects how much was executed rather than read. Finding IDs `F-nn` are in this report; other IDs (`FIN-`, `VAL-`, …) are in Appendix D / the cluster reports.

| Engine (module · quantity owned) | Accuracy | Relevance | Conf. | Worst finding |
|---|---|---|---|---|
| `engine.py` financial layer · treasury, CSF pool, CapEx allowance + term loan, OPEX transients, NCD, CAROIC, floor | **Sound inside `process_tick`**: conservation residual 0.00 on all five shipped paradigms × 3 compositions (probe `fin/law_all_paradigms.py`); loan amortises and clears at R10; transient OPEX surcharges reverse to the cent (B-2 closed). Defects: a negative treasury is charged interest twice per round (engine debt service + balance-sheet revolver) plus a covenant surcharge, ≈22–33% per 6-month round (FIN-03); `FINANCIAL_TREASURY_FLOOR` enforced only inside `process_tick`, bypassed by post-tick writers (FIN-10); "emergency credit" charges $140k/round interest on $1M that is never credited (FIN-05). | Tax 25% and CBAM $100/t defensible; 12% *per round* term loan ≈24% p.a. vs 6–9% real; deficit pricing ~30% p.a. vs 10–15% DIP; 47% EBITDA margin on a $50M-treasury group means ±$5M of CapEx cannot register against it — CapEx share 5–60% of pool does not move treasury or TV monotonically and seed noise exceeds the lever; only 100% of the pool separates (FIN-15). | High | F-13 (FIN-03) |
| `balance_sheet.py` · statement, RE, covenants, revolver | A = L + E holds to the cent in 200/200 statements — **because retained earnings is a plug** (`balance_sheet.py:858`, `imbalance` hard-coded 0.0; RE roll-forward residual up to $157M in one round, FIN-07). CapEx term loan never reaches the statement in production wiring (F-12). Statement struck before NPC fines/agent costs so cash ≠ treasury by up to $24.5M incl. the R10 statement (F-08). A pre-commit `GET /balance-sheet` ticks and persists R1 twice (FIN-04). | Standard structure; P&L and balance sheet do not articulate; net income printed beside a plugged RE. | High | F-12 (FIN-01) |
| `terminal_valuation.py` + `round_logic._post_r10_grand_finale` · M_R, terminal value, equity bridge, share price | **One arbiter at the finale** for all paradigms (`round_logic.py:2649`; 52 tests pass); clamp [0, 2.05] enforced; ceilings 1.93/2.02 reachable (`val/probe_mr_bounds.py`). Mid-game preview, Consequence-DNA and What-If feed `calculate_mr` the raw flag dict so list-held flags are invisible — 370/384 grid states diverge, up to −0.57 (F-15). Share price on a $50 IPO / 100M-share scale → every team red (VAL-02). NCD index (0–5000) rendered as dollars and subtracted from dollars in the solvency gate (VAL-03). R10 "Divest" resets synergy to 1.0 and passes the ≥0.80 gate (VAL-07). Climate +0.15 bonus dead — `baseline_ci` never passed (VAL-08). `brsr_ngrbc` sessions never reach the BRSR finale from `commit_turn` (VAL-06). | Gordon multiple 11.5–13.3× at WACC 9.7–10.9% sits inside MODEL_CARD's 6–18 range and real sector EV/EBITDA; −0.40 instability discount fires in 6/6 probe games and 10/11 bot runs (avg SLO 0–28) so it discriminates nothing and "Regenerative Titan" (≥1.8) is unreachable in practice (VAL-10). | High | F-15 (VAL-04) |
| `ending_pathways.py` · pathway M_R deltas, archetype ladder | Ramped, clamped once; two solvency definitions → "Safe-Haven" on the reveal and "Hollow Idealist / equity wiped out" on the card for the same player (VAL-09). | Banding 1.8/1.2/0.8 on a ramped M_R is teachable; the only $-cliff is the treasury-sign gate. | Med-High | VAL-09 |
| `engine.apply_natural_decay` · per-BU SLO and reputation tiers | Arithmetic and clamps correct; shipped tiers 0.15/0.20/0.30, floor $100k, growth escape $3M (runtime print). **The tier is evaluated on the group-average investment ratio, not the BU's own** (`engine.py:3089`, `4632-4634`, `4702`): a $0 BU grows +3/round when the team average clears 0.30; every 09-03 calibration number was measured per decision, a quantity the engine never evaluates (F-17). | Step function on a company-wide spending band; neglect −4%/round, full investment +3..+10/round; the mild tier is a narrow band (80–120% of pool with no BU ≥$3M), not a dead lever. Docs still describe the reverted 0.10/0.25/0.50 + $500k version in four places (SOC-8). | High | F-17 (SOC-2) |
| `engine.py` greenwash block · scandal −15 SLO/BU | Thresholds correct (full 0.15, moderate 0.1005, $3M/BU escape); player messages state the wrong bar and "no green option selected" for teams that passed via the escape (SOC-7); a scandal is not a betrayal flag for NPC memory (SOC-4). | Plausible magnitude; R3 checks every team (all three options "full"). | High | SOC-7 |
| `engine.py` stakeholder fatigue (F-08) | **Dead**: baseline captured after between-tick reconciliation, no in-tick reputation gain exists; 0/360 runs fire; MODEL_CARD/CHANGES claim it was fixed (SOC-3). | n/a | High | SOC-3 |
| `npc_stakeholders.py` · 4 NPCs: satisfaction, trust, escalation, regulator fine | **Fine draw is unseeded in production** (`npc_stakeholders.py:651/693` read `new_global` flags, which are this round's events; seed merged only afterwards `router.py:3005-3008`) — identical teams $24.4M / $7.4M / $15.4M (F-04). Patience clock forces escalation from tier 1, and tier 0 needs satisfaction ≥70 for all four NPCs, so every team is forced hostile every 3 rounds (F-16). Enforcement message quotes a different amount than deducted (RNG-6). Not visible to players or facilitator (F6 rail OFF; no admin view). | $5–25M repeatable per round ≈17% of annual revenue vs 2–5% of turnover caps in real regimes (FIN-09); the fine vector is identical across opposite strategies in 9/12 seeds — it is seed-sized, decision-linked two rounds earlier. | High | F-04 (RNG-1) |
| `autonomous_agents.py` · 5 agents, treasury/reputation/SLO/OPEX/CoC hits | Seeded; patience only arms from "watching"; dormant reachable; threshold offsets are a constant, not per-cohort (RNG-7). | Plausible; displayed in the cockpit panel; earliest "triggered" is R4 under maximal breach, not R6 as the teleprompter says. | Medium (sampled) | RNG-7 |
| `org_politics.py` · C-suite satisfaction | Display-only — never touches treasury/OPEX/SLO/reputation; `apply_coalition_outcome` has no caller; coalition-check endpoint unseeded and uncalled. | UI copy says "losers slow-walk delivery" and promises "coalition fragmentation data that predicts labour unrest" — neither exists (SOC-5). | High | SOC-5 |
| `stakeholder_sentiment.py` · attitude scores | 58-flag rule map never receives a decision flag (caller passes only boolean event keys) — narratives never fire (FLAG-3). | Display-only; NPC bridge weight 0.15–0.30. | High | FLAG-3 |
| `stakeholder_engagement.py` · promise ledger | ON by default; no player entry point (SOC-6). | n/a | High | SOC-6 |
| `rng_util.py` + engine-side draws · black swans, shocks, overrun, FX, macro noise, fog, R5/R9 rolls | **All engine-side draws seeded from stored flags**; global reseed (B-1) gone; two identical teams → 0 field diffs over 10 rounds with the NPC draw stubbed (`rng/probe_full_identical.py`). Facilitator `rng_seed` never reaches live state (RNG-2); `replay.py` PARTIAL by construction (RNG-4). | Across cohorts, luck SD on final treasury ($20–26M) is 2–5× the gap between the two viable strategies and the ranking flips between seeds; reputation/SLO/M_R are decision-dominated (ratios 0.04/0.00/0.01). Within a cohort luck is shared (F-09). | High | F-04, F-09 |
| `black_swan_registry.py` · 13 events, difficulty tiers | Revenue/OPEX % applied as permanent base erosion with no reversal although narratives say "for N rounds"; headline "treasury hit" understates 4–5× (IMP-05). Tier table keyed `easy/standard/expert` while sessions carry `foundation/advanced/expert` → foundation ≡ advanced financially; 4 of 8 tier keys never read (FIN-08). `ethical_ai_overhaul` −5pp and `supply_chain_transparency_avg` modifiers read keys never written (FLAG-8, IMP-11). | Treasury hits 5–8% inside the card; permanent erosion undocumented. | High | IMP-05 |
| `systemic_risk_engine.py` · ESG WACC, tipping, materiality shocks, cascades | ESG WACC wired, right direction, clamped [3%, 20%]. Social tipping tests SLO in the wrong direction (IMP-10). Materiality shock fires a blocking critical modal with no state effect and no link to the team's own matrix (IMP-07). Tipping penalties compound per round while docs say one-off (IMP-16). | WACC premia ~10× ESG-spread literature (stated pedagogy). | Med-High | IMP-07 |
| `biodiversity_engine.py` · EHI, ESV, "Nature's Invoice" | EHI falls 65 → 2.5 for every team regardless of decisions (constant deforestation + drift; NCD wire reads an events key nobody writes) and charges treasury every round from R2 (F-19a). | Indefensible as shipped; greenest team goes red at R4. | High | F-19 |
| `impact_engine.py` · R5 cyclone/resilience, R9 friction/strike/retraining | Rolls seeded; R5 resilience factor protects nothing after R5 (no later physical hazard reads it; R10 `climate_resilience` constant 0.5) while the option text promises protection "any future round" (FLAG-6/IMP-09); pillar-mode R9 clawback message false (IMP-15). | R9 friction bounded and live. | High | FLAG-6 |
| `sdg_linkage_engine.py` · SDG alignment | Two arbiters in one payload (engine group score vs router-recomputed BU scores, contradictory alerts); 4 of 8 inputs are constants from wrong keys (F-19d). Display-only. | Stylised. | High | IMP-08 |
| `meadows_leverage.py` · leverage-point analysis | Always "LP12 · 0% · low-leverage" (no writer of `decision_history`; wrong call signature swallowed as TypeError) (F-19c). Display-only, shown in the CEO interview and DNA "System Effectiveness". | Invented metric, max 37.8% even with perfect input. | High | IMP-03 |
| `tcfd_scenarios.py` · scenario carbon cost / VaR | Emissions proxy ×69 (CI × BUs × 1000 assumes $1B/BU) → "annual carbon cost" 85–120% of revenue on the TCFD tab (F-19b); demand shift squared; ZeroDivision at revenue 0. Display-only. | Indefensible as shipped. | High | IMP-02 |
| `turnaround_engine.py` · post-R10 arc | Placeholder calibration; every lever injects cash ($34–70M free over 4 rounds vs $50M initial capital); direction (trust before cash) right; opt-in, off by default (IMP-14). | Outside any reference. | Medium | IMP-14 |
| `pedagogical_engine.py` · toggles + content | Analytics functions have no caller; nothing it computes reaches participants; MidGameCheckpoint renders nothing; CalibrationReport fetches a route that does not exist (IMP-17). | Content only. | Medium | IMP-17 |
| `market_dynamics.py` | Zero importers — dead module. | n/a | High | IMP-17 |
| `supply_chain_network.py` · visibility, disruption, audit | Effectively display-only; disruption roll unreachable (initial visibility 0.482 > 0.40 gate) and unseeded; audit endpoint has no UI caller and re-charges $500k per repeat (OPS §C). | n/a | Medium | RNG-8 |
| `consequence_dna_api.py` · Sankey, impact score | Live endpoint builds every session's graph at round 1 (`router.py:3524` passes a state without `round_number`) — flags dropped, ignition impossible; the R10 snapshot is correct (SEAM-11). | Display-only. | High | SEAM-11 |
| `round_logic.py` post-tick handlers + flag taxonomy · deferred consequences | **EU AI Act R7+ charge does fire** (refutes the prior lead; `_apply_eu_ai_act_liability` runs from `post_tick` every round, probe R7 −$3M, R8–R10 −$1M). Desalination $5M/round never matures — counter reaches 1 at R10 (F-05). 381 flags set / 187 never read, 31 of them in core configs with no taxonomy ruling; 46 read-never-set incl. `greenwashing_detected` (five dead readers) (FLAG-9). `multi_toggles` persists the legacy proxy option's flags — an omitted pillar area silently sets `insurance_only` etc. and blocks +0.20 M_R (FLAG-2). Adaptive crisis severity computed, announced, never applied (FLAG-5). | Promises vs delivery: 5 kept (early_decarboniser, synergy_unlock, community_fund, resilience block, R2 tier), 11 not (list in F-05 and Appendix D). | High | F-05 (FLAG-1) |
| `pillar_configs.py` / `round_configs.py` · option payoff matrix | Impacts apply as declared; text contradicts code in seven places (75% vs 0.50 strike; +0.35 vs 0.30 synergy; "over 3 rounds" applied once ×2; "NCD forgiveness +25%" has no formula term) (FLAG-10). | Options move TV ≥0.5% (BALANCE_REPORT §3 reproduced byte-identical at HEAD). | High | FLAG-10 |
| `config.py` chain · precedence, clamps, hot reload | Precedence as documented: image copy seeds the volume once, then the **volume wins forever** (`runtime_paths.py:273-279`); clamps loud for CBAM/NCD/imitation/slo-order; **`regulatory_ratchet.baseline` unclamped** (F-07). Excel importer rebuilds the whole file, drops 17 keys, silent on negatives/out-of-range; shipped xlsx is stale (0.1 imitation) (CFG-04/06). Hot reload leaves `router.py`'s captured `DEFAULT_IMITATION_DECAY_RATE` stale (CFG-05). `/config/live` reports `in_sync` on a stale volume and `healthy:false` on a pristine one (CFG-03). Provenance sidecar names 5 keys that do not exist and is read by nothing. | n/a | High | F-07 (CFG-01) |
| `validation_logic.py` | **Not opened** beyond grep (harness with its own random draws; does not score M_R/TV). | — | — | — |
| `branching_engine.py`, `journey_improvements.py`, `regional_reporting.py`, `round_recap_engine.py` | Sampled: archetype branch modifiers unconsumed (FLAG-5); R6/R7/R8 journey panels promise effects never applied and PATCH a super-admin route as a player (IMP-04); regional ESG report prints Scope 1+2 = 0 GREEN (IMP-12); `generate_round_recap` uncalled (IMP-17). | Display-only. | Medium | IMP-04 |

---

## Findings (20, ranked by classroom and pedagogical impact)

Severity: P0 class-stopper or false teaching · P1 session-degrading · P2 fix before next cohort · P3 hygiene. Every finding lists the two methodologically distinct passes used and what was tried in order to dismiss it. Paths are relative to the repo root; `~/audit_scratch/` refers to the companion archive.

### P0

#### F-01 · Every freshly issued participant is refused every write after sign-in, and no screen tells them why — P0 · CONFIRMED · F1/C5
**Evidence.** `backend/router.py:351-359` inside `_assert_player_owns_session`: `if not tok.get("observer") and request.method.upper() not in ("GET", "HEAD", "OPTIONS") and await _player_must_change_password(...): raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail={"code": "password_change_required", "message": "Set a personal password before playing — the initial one is only for signing in."})`. Players are minted with `must_change_password: True` (`admin_router.py:5469`) and password `MUR-NNNN@123` (`default_credentials.py:8`). The only renderer of the change-password dialog is `JoinCohortModal` (`grep -rln "<ChangePasswordModal" frontend/app` → `JoinCohortModal.js` only; `JoinCohortModal.js:240-249`), and the modal is mounted only while there is no session: `frontend/app/page.js:1535` `{!sim.sessionId && <div className={styles.joinOverlay}><JoinCohortModal sim={sim} /></div>}`. Login sets both in one batch: `hooks/useSimulation.js:414` `setSessionId(playerSessionId);` … `:435-437` `if (loginData.must_change_password) { setMustChangePassword(true); }`. The commit path swallows the 403: `useSimulation.js:555-558` `if (denied?.detail?.code === 'password_change_required') { setMustChangePassword(true); setLoading(false); return null; }` — nothing consumes that state afterwards (`grep -rn mustChangePassword frontend/app` → JoinCohortModal and the hook only). The R1 gate shows a misleading error: `StakeholderMapModal.js:274` `setResult({ error: true, message: 'Server returned incomplete data. Please retry.' });`. The backend comment at `router.py:349-350` ("the cockpit needs the board to render the forced change-password modal") describes an intent the cockpit does not implement; `CHANGES_launch_blockers_2026-09-02.md:147-149` claims "The cockpit re-opens the change-password modal on that code" — false.
Probe (`~/audit_scratch/flow/probe_pwchange.py`, re-run by the lead 2026-09-04 05:xx UTC): `player-login 200 {... 'must_change_password': True ...}` → `stakeholder-map POST 403 {"detail":{"code":"password_change_required",...}}`, `save-decisions POST 403`, `commit-turn POST 403`, `set-username ... -> 200`, `change-password 200` → `save-decisions after change 200`. Jest (`~/audit_scratch/flow/jest/pwmodal.test.js`, rendering `app/page.js` with `sessionId` set and `mustChangePassword: true`): no element `change-password-title`, no text matching /password/, briefing rendered.
**Failure scenario.** 50 participants sign in with `MUR-XXXX` / `MUR-XXXX@123` → username prompt (passes) → briefing → "Open Stakeholder Map" → place ≥6 stakeholders → Submit → "Server returned incomplete data. Please retry." on both allowed attempts. Nobody can commit R1. Likelihood at 50 users: 100% of fresh credentials. Time to detect: first submit (~10 min into the session). Time to recover by a non-developer: ~2 min per person (log out, "Forgot your password?", set a personal password, sign in again) — ~100 person-minutes for the room if nobody was pre-briefed.
**Why it matters.** The whole room has no forward action; the facilitator has no button that clears the flag (reset-password re-arms it, FLOW-07).
**Fix (minimal).** `frontend/app/page.js`, before the username/briefing early returns (~line 1371):
```jsx
+ import ChangePasswordModal from './components/ChangePasswordModal';
  ...
+ if (sim.sessionId && sim.mustChangePassword) {
+   return <ChangePasswordModal isOpen isForced prefillPlayerId={sim.playerId || ''}
+            onClose={() => {}} onSuccess={() => sim.setMustChangePassword(false)} />;
+ }
```
and surface `detail.message` on 403 in `StakeholderMapModal.js:274` and the commit handler.
**Cheaper pre-class mitigation (verified at the API level).** Slide 1: "Click **Forgot your password?** (`JoinCohortModal.js:173-176`), enter your team ID, current password `ID@123`, choose a new password (≥8 chars), then sign in." Anyone already signed in: Log out first.
**Passes.** (a) static read of the guard, the hook and the only renderer; (b) executable probe of every player write with the default password, re-run by the lead; (c) jest render of the real page; (d) history — enforcement landed in `ad8ccf6` (2026-09-02), the UI consumer dates from 2026-06-18.
**Dismissal attempts.** Looked for any other `ChangePasswordModal` mount, for an early return keyed on `mustChangePassword` in `page.js` (none — the early returns are username :1371 and briefing :1395), for a player-side master-password bypass (`router.py:968` is facilitator-only), and for `must_change_password` being false on generate-player (it is True). Survived.

#### F-02 · The facilitator's Round Debrief cards show the NEXT round's decision under each round's heading — P0 · CONFIRMED · F3
**Evidence.** Decisions are logged under the round they were made in: `database_memory.py:1239` `_decisions_round = max(1, round_number - 1)` (Postgres identical, `database.py:1377`). The debrief endpoint labels history row `rn` as the decision made in `rn - 1` — `admin_router.py:8513-8514` `# The decision was made in (rn - 1), producing state at rn` / `decision_round = rn - 1` — but reads the decisions made in `rn`: `admin_router.py:8555` `round_decisions = decisions_by_round.get(rn, [])`. Rendered as "Decision Made … {choiceLabel} — {round.choice_title}" (`DebriefReport.js:386-388, 404-409`), mounted on the facilitator and god-mode pages. Probe (`~/audit_scratch/seam/compare.py`, cohort `38d7f460…`, player alpha): debrief says R1 option_a / R4 option_b / R6 option_a / R8 option_c / R9 option_a; the commit log says R1 option_b / R4 option_a / R6 option_b / R8 option_a / R9 option_c; the latest played round always shows `choice_selected: ''`. The participant dashboard has the same shift: `router.py:2159` `out[tag - 1] = ch` (consumers `SustainabilityBalancedScorecard.js:2108`, `StudentReportExport.js:23`).
**Failure scenario.** Facilitator projects "R9 Just Transition — A: Immediate Closure" at a team that funded the community; the per-round deltas on the same card are correct, so the wrong decision is "explained" by the right numbers. Likelihood: every card, every team. Detect: only if a team objects. Recover: none during class except switching surfaces.
**Fix.** `admin_router.py:8555` `decisions_by_round.get(decision_round, [])`; `router.py:2159` `out[tag] = ch`. **Mitigation.** Read decisions from `active_event_flags.rN_choice` or admin analytics `decision_impact.choice` (`admin_analytics.py:693-698`, verified correct R1–R8).
**Passes.** (a) static read of tagging + endpoint + renderer; (b) programmatic comparison of the surface against the commit log. **Dismissal.** Checked whether `DebriefReport.js` re-derives from flags (it does not, :375-376); Postgres tags identically.

#### F-03 · Reputation 0 is rendered as 50 on the Debrief Narrative and on 18 participant surfaces; the results overlay says "reputation rose 23 points" when it fell to 0 — P0 · CONFIRMED · F3
**Evidence.** `admin_debrief_narrative.py:46` `"reputation": float(gs.get("group_reputation", 50) or 50),` — `0.0` is falsy; the turning-point picker (:58-75) then reports the largest swing of the corrupted series. `ExecutiveCockpit.js:482` `const reputation = globalState?.group_reputation || 50;` (KPI belt :1680); results overlay `:2621-2623` `const nR = commitResults.globalState?.group_reputation || 50; … const dR = nR - reputation;` → "Treasury … and reputation rose/fell N points" (:2630-2633); same idiom at ExecutiveCockpit 590/597/615/632/4944-4947, AnnualReport.js:41, StudentReportExport.js:18, TerminalValuationCalc.js:51, WhatIfSandbox.js:45, MarketTicker.js:53, AIAdvisor.js:43, ESGImpactConstellation.js:60-61, stockValuationEngine.js:208, DecisionHistory.js:51. Reputation 0 is reachable — engine clamps at 0 (`router.py:3293`); 15 of 20 bot runs with option_b throughout ended R10 at 0 (`~/audit_scratch/soc/p8b.log`). Probe (seam, player beta): persisted reputation 0.0 from R4 on seven surfaces; narrative `final.reputation 50.0`, `turning_point {round:5, metric:reputation, delta:23.0, direction:up}` (actual R4: 27 → 0).
**Failure scenario.** The projected narrative card says "Turning point R5: reputation +23 up" for a team whose reputation collapsed to zero; the team's own belt shows 50/100. Likelihood: any team that hits 0 (common under weak play). Detect: never, unless compared with the admin leaderboard (which is correct: `admin_router.py:6205` uses `.get` default).
**Fix.** `?? 50` / `if x is None` at all 19 sites (narrative: `_r = gs.get("group_reputation"); float(50 if _r is None else _r)`). **Mitigation.** Do not project the narrative's reputation column or turning points; read reputation from the admin leaderboard.
**Passes.** (a) static (grep of the idiom); (b) probe on a session with persisted 0.0. **Dismissal.** Looked for a normalisation layer between API and belt (none; belt reads `globalState` directly).

#### F-04 · The regulator's $5–25M fine is drawn from the unseeded global RNG per team — identical teams in one cohort end up to ~$17M apart — P0 · CONFIRMED · E4
**Evidence.** `backend/npc_stakeholders.py:651` `_seed = gs.get("active_event_flags", {}).get("stochastic_seed")`; `:691-694` `fine = (_seeded_fine(_seed, npc_id, round_number, 5_000_000, 25_000_000) if _seed not in (None, "") else round(random.uniform(5_000_000, 25_000_000), 2))` / `gs["corporate_treasury"] = round(... - fine, 2)`. The `gs` handed in is `new_global` (`router.py:2919-2924` `run_new_engines(round_number=current_round, global_state=new_global, …)`), whose flags are this round's events dict — `engine.py:4534` `"active_event_flags":    events,` — and the stored flags (where the seed lives) are merged in only afterwards: `router.py:3005-3008` `merged_flags = dict(current_global.get("active_event_flags", {})) … new_global["active_event_flags"] = merged_flags`. `grep -n stochastic_seed backend/engine.py backend/round_logic.py` → no hits, so the seed is never in `events`. The offline harness masks it: `dry_run.py:191` copies the seed forward before `post_tick`. Probe (`~/audit_scratch/rng/probe_diverge.py`): one cohort, three players, byte-identical decisions R1–R4 → R4 `regulatory_fine 24,381,543.06 / 7,432,545.39 / 15,363,949.43`, treasury `55.8M / 72.8M / 64.8M`. Control (`probe_full_identical.py`, NPC draw stubbed): 0 diffs over 10 full rounds. Fix verification (`probe_fix_verify.py`): with the seed forwarded, all three teams get fine `17,078,216.61`. Enforcement was reached at R4 in 48/48 bot games and still at R4/R7 with a perfect R1 stakeholder map (`probe_perfect_map.log`).
**Failure scenario.** Two teams commit identical R4 decisions; one drops $24.4M, the other $7.4M; the peer leaderboard (ranked on treasury, `router.py:5364`) reorders them; nothing on screen shows the fine (no player surface renders `regulatory_fine`). Likelihood: every cohort, at least once. Detect: never from the UI. Recover: none.
**Fix.**
```diff
--- a/backend/router.py   (immediately before `post_events = post_tick(` at ~2901)
+    _seed_fwd = (current_global.get("active_event_flags") or {}).get("stochastic_seed")
+    if _seed_fwd:
+        new_global.setdefault("active_event_flags", {}).setdefault("stochastic_seed", _seed_fwd)
```
Side effect to state in the commit: NPC threshold offsets and agent jitter become per-cohort (currently a constant, RNG-7). **Mitigation.** Switch off `npc_stakeholders_enabled` per cohort (removes all NPC reactions).
**Passes.** (a) static read of the flag plumbing (re-read by the lead); (b) three-team divergence probe + stubbed control + fix verification. **Dismissal.** Looked for the seed in `ctx.events`, in `seed_effective_flags` (climate keys only), and for any test exercising the fine through the router (none). Survived.

#### F-05 · R8-C "Desalination Mega-Project" promises $5M/round for three rounds; $0 ever arrives — P0 · CONFIRMED · E7
**Evidence.** Player text `round_configs.py:588-592`: "… 2-round construction delay before benefits arrive … Generates $5M/round revenue from Round 10 onward (Rounds 8+2) for 3 rounds."; impacts `:602-603` `"generates_revenue": 5_000_000, "payback_rounds": 3,`. Handler `round_logic.py:2266-2274` appends one `pending_capex_projects` entry `{"type": "revenue_generation", "amount": gen_rev, "rounds_remaining": payback_rounds}`. Consumer `engine.py:2335-2337` `p = {**proj, "rounds_remaining": proj["rounds_remaining"] - 1}` / `if p["rounds_remaining"] <= 0:` — fires once when the counter reaches 0; the game ends at `SIM_ROUNDS` = 10. Probe (`~/audit_scratch/flag/probe_deferred.py`, R8-C over a real R1→R10): queue after R8 `rounds_remaining: 3`, after R9 `2`, after R10 `1`; no $5M credited; treasury R8 47.7M → R10 −40.6M. The facilitator script states three different numbers: `admin_teleprompter.py:570` "$15M revenue payback"; `FacilitatorTeleprompter.js:1320` "$3M/round payback for 3 rounds". Even if it matured it would be one lump (`engine.py:2364-2365`), not "$5M/round".
**Failure scenario.** A team spends $30M in R8 told $15M returns; nothing returns; in the probe the team is insolvent from R9. Likelihood: every team choosing R8-C (a defensible "sustainable" choice). Detect: only by the team doing the arithmetic. Recover: none.
**Fix.** `round_configs.py:603` `"payback_rounds": 2` (matures at the R10 tick like the NCD drop, which works), text → "a single $5M payment in Round 10"; fix the two teleprompter strings. **Mitigation.** Before R8: "the desalination payback does not land inside the game."
**Passes.** (a) static config → handler → pending-project engine (re-read by the lead); (b) full-run probe. **Dismissal.** No other consumer of `generates_revenue`/`payback_rounds`; no post-R10 tick; `terminal_valuation` never reads pending projects.

#### F-06 · "The Road Not Taken" counterfactuals are a pipeline artifact — identical for every alternative and equal to the post-tick deductions — P0 · CONFIRMED · F3
**Evidence.** `router.py:3319-3337`: the shadow run is `process_tick(...)` only, and `regret_analysis[alt_option] = {"treasury_delta": round(shadow_gs["corporate_treasury"] - new_global["corporate_treasury"], 2), …}` where `new_global` has already been through `post_tick` (`router.py:2901`) and `run_new_engines` (`:2919`). Consumers: `EngineEventsPanel.js:702-703` "Here's what the alternatives would have yielded" and `FrontPageReveal.js:1021-1046` (caption :1044 "Counterfactual outcomes computed by the group's own planning model."). Probe (seam, player alpha; gap = `waterfall.final_treasury` − actual): R4 gap 30,022,303.52 → regret option_b +30,022,303.52, option_c +30,022,303.52; R9 gap 21,933,383.74 → both alternatives +21,933,383.74; R10 gap 14,395,662.82 → both +14,395,662.82. Equal to the post-tick deductions to the cent in 9/10 rounds. Alpha R10: `r10_choice=option_b` (synergy-gate fallback) while the panel says "You chose Option A".
**Failure scenario.** Every round the panel tells every team that both alternatives would have left them $20–30M richer — the number is the fine/agent/option cost the pipeline deducted after the shadow run, not a counterfactual. Likelihood: every round, every team. Detect: never.
**Fix.** Diff the shadow against `tick_result["global_state"]` (`router.py:2811`), or run the shadow through the full pipeline. **Mitigation.** Hide `decision_regret` and the Front Page "Road Not Taken" section.
**Passes.** (a) static (re-read by the lead: 2901/2919 precede 3319); (b) per-round numeric probe. **Dismissal.** Looked for a re-baselining between 3337 and the response at 3370 (none).

#### F-07 · `regulatory_ratchet.baseline` is the one stale-volume value with no clamp; on a stale volume every team is fined $1.6–3.75M in round 1 — P0 if the production volume is stale · mechanism CONFIRMED, production state NOT VERIFIED · E5/C3
**Evidence.** `git diff 81c23f9 HEAD -- simulation_config.json` changes 8 value-bearing keys incl. `regulatory_ratchet.baseline 10.0 → 20.0`, `cbam.surcharge_rate 100000 → 100`, `imitation_decay.default_rate 0.1 → 0.05`. The volume copy wins forever once seeded: `runtime_paths.py:273-275` `target = data_dir() / name` / `if target.exists(): return target`; `config.py:139` `CONFIG_PATH = _config_file("simulation_config.json")`; `Dockerfile:67` `ENV MURESSONS_DATA_DIR=/data`. Clamps exist for CBAM (`config.py:481`), NCD (`:228`), imitation (`:689`); none for the ratchet: `config.py:493` `REG_RATCHET_BASELINE: float = float(_reg.get("baseline", 20.0))` with nothing following. Consumer `engine.py:4301-4305` `regulatory_baseline = REG_RATCHET_BASELINE + (round_number // 2) * REG_RATCHET_ROUND_INCREMENT` / `if avg_gov_risk > regulatory_baseline: fine = (avg_gov_risk - regulatory_baseline) * REG_RATCHET_FINE_PER_POINT; ctx.new_treasury -= fine`. Probe (`~/audit_scratch/cfg/probe_e2e_cbam_tax.py`, scratch volume with `baseline=10.0`): R1 `{"active": true, "baseline": 10.0, "fine": 1625000.0 …}`; clean dir `{"active": false, "baseline": 20.0, "fine": 0}`; the only `[CONFIG]` line printed was the imitation clamp. Seed governance risk mean 13.25 (generic) / 17.5 (healthcare → $3.75M). Whether a stale copy exists on production is asserted by the repo (`CHANGES_launch_blockers_2026-09-02.md:600-601` "simulation_config.json there still says 100 000 for CBAM") but the Dockerfile `COPY simulation_config.json` line (`1c0c858`) is not in the production tip, so a volume copy can only have come from an earlier upload/save — **unknowable from here**.
**Failure scenario.** Class starts on `d3bbf09` against a volume seeded before 2026-09-01: CBAM/NCD/imitation are clamped with a stdout WARNING nobody reads; the ratchet is not; every team loses $1.6–3.75M before any decision and the R1 event card shows a "Regulatory Ratchet … Compliance fine". Detect: R1 event card. Recover: none mid-class (config change + restart).
**Settling command (owner, Railway CLI on the production account).** Inspect: `railway ssh -- python3 -c "import json;c=json.load(open('/data/simulation_config.json'));e=c['engine_parameters'];print('cbam',e['cbam']['surcharge_rate'],'imit',e['imitation_decay']['default_rate'],'ratchet',e['regulatory_ratchet']['baseline'],'synergy',e['synergy'].get('max_reduction_per_round'),'ncd',c['ncd_parameters'].get('hard_cap'),c['ncd_parameters'].get('warn_threshold'),c['ncd_parameters'].get('opex_penalty_per_unit'),'slo_ramp',e.get('slo_ramp'))"` — success = `cbam 100 imit 0.05 ratchet 20.0 synergy 0.06 ncd 5000 1000 1000`; "No such file" = nothing to do (the HEAD image seeds the correct copy). Patch (same 7 keys as `scripts/patch_volume_config.py:15-23`, which is NOT in the image — Dockerfile copies only `backend/`, `db/`, `simulation_config.json`): `railway ssh -- python3 -c "import json,shutil;p='/data/simulation_config.json';shutil.copy2(p,p+'.bak-preclass');c=json.load(open(p));e=c['engine_parameters'];e.setdefault('cbam',{})['surcharge_rate']=100;e.setdefault('imitation_decay',{})['default_rate']=0.05;e.setdefault('regulatory_ratchet',{})['baseline']=20.0;e.setdefault('synergy',{})['max_reduction_per_round']=0.06;n=c.setdefault('ncd_parameters',{});n['hard_cap']=5000;n['warn_threshold']=1000;n['opex_penalty_per_unit']=1000;json.dump(c,open(p,'w'),indent=2);print('patched',p)"`, then Restart, re-inspect, and confirm no `[CONFIG] WARNING` in the deploy log.
**Fix (code).** After `config.py:493`: clamp `REG_RATCHET_BASELINE <= 10.0` back to 20.0 with a `[CONFIG] WARNING`, mirroring the CBAM pattern at :481.
**Passes.** (a) static (re-read by the lead); (b) executable probe on clean vs stale scratch volumes; (c) `git diff` of the config between production tip and HEAD. **Dismissal.** Looked for a ratchet clamp, paradigm gating of the fine (none — lines 4297-4304 sit outside the `advanced_climate` block), and god-mode/cohort overrides of the key (none).

### P1

#### F-08 · Three different "closing treasury" values: finale figures are struck mid-pipeline, the live game-over screen renders the state ENTERING round 10, and the balance sheet is struck before the round's fines — P1 · CONFIRMED · F3/E1
**Evidence.** (i) `_post_r10_grand_finale` runs inside `post_tick` (`router.py:2901-2909`) and stamps `extra["final_treasury"] = gs["corporate_treasury"]` (`round_logic.py:2933`), `extra["group_reputation"] = round(gs.get("group_reputation", 0), 2)` (:3000), `net_debt = round(total_financial_debt - treasury_cash, 2)` (:2766-2767) → equity/share price (:2778-2779); `run_new_engines` (`router.py:2919`) then applies the R10 agent hits. Consumers: `FrontPageReveal.js:479` `['Closing treasury', money(data.final_treasury)]`, :470 group reputation, `GameOverSummary.js:127-144` equity/share price. Probe (seam, player alpha): persisted treasury 70,584,400.85 | `final_treasury` 79,980,063.67 | `consequence_waterfall.final_treasury` 84,980,063.67; reputation 14.72 persisted vs 45.72 on the Front Page. (ii) `hooks/useSimulation.js:613-615` `if (data.events?.profile || data.new_round_number > 10) { setGameOver(true); setFinalReport(data.events); }` — no `setGlobalState`; the poller exits on `gameOver` (:730); the reveal reads `sim.globalState` (`page.js:1182-1185`) → the live reveal shows the R10-entry state (59,473,213.42) while a reload shows 70,584,400.85. (iii) Balance sheet: `run_new_engines` order is biodiversity :695 → balance sheet :743 → … → NPC tick :843 → agents :1006; `balance_sheet.py:566` copies treasury into cash before the fine at `npc_stakeholders.py:694`; 50 of 200 round statements show `cash != max(0, treasury)` (`~/audit_scratch/fin/recon_full.py`), e.g. `advanced_climate advanced R10 bs_cash=23,418,995.75 treasury=1,546,951.40`.
**Failure scenario.** The projected Front Page says closing treasury $80.0M; the team's belt says $70.6M; the live reveal said $59.5M five minutes earlier; the balance-sheet modal says cash $79.9M. Likelihood: every team at R10. Detect: immediately if two surfaces are compared. Recover: reload the reveal (fixes ii only).
**Fix.** Re-stamp `final_treasury`, `group_reputation`, net debt/equity and `carbon_tonnage_group` after `run_new_engines` (before `merged_flags`, `router.py:3005`); in the game-over branch `setGlobalState(data.global_state); setBusinessUnits(data.business_units)`; move the balance-sheet block (`round_logic.py:709-766`) after the agents block (:1035). **Mitigation.** Read closing treasury and reputation from the dashboard belt or the admin leaderboard; ask teams to reload before reading the reveal; never read the Front Page P&L block or the statement's cash line aloud.
**Passes.** (a) static read of the pipeline order; (b) programmatic comparison across surfaces and the 20-run reconciliation. **Dismissal.** Searched for a post-engine re-stamp of `final_treasury` in `router.py` (0 hits) and for `fetchDashboard` on game over (none).

#### F-09 · Treasury and terminal value are luck-heavy across cohorts; reputation, SLO and M_R are decision-dominated — rank and debrief on the latter — P1 · CONFIRMED (measured) · E6/E4
**Evidence.** Monte-Carlo through the real router (`~/audit_scratch/rng/mc_luck.py`, `mc_results.jsonl`, `mc_analyze.py`): one fixed "balanced" path across 34 cohort seeds → final treasury mean $66.3M, **SD $20.3M** (range $5.6M–$95.8M); "green" path across 14 seeds → mean $71.3M, SD $26.0M (−$4.3M to $94.0M — the best strategy goes bankrupt in one cohort). Between the two viable strategies the treasury gap is $8.3M (seed 1) and −$4.4M (seed 2: ranking flips). Terminal value SD $84–149M vs viable gap $268–308M (ratio 0.31–0.56). Reputation / avg SLO / M_R: SD_luck/range_decisions = 0.039 / 0.000 / 0.009; path ranking identical at both seeds. Drivers: corr(treasury, n_swans) −0.54, corr(treasury, fines) −0.48; 1.94 swans/game. Within one cohort the swan/FX/overrun luck is shared (all seeded, `rng/probe_full_identical.py`: 0 diffs), so an intra-cohort treasury board is fair except for F-04.
**Why it matters.** Any cross-cohort or "benchmark" reading of treasury/TV, and any lesson of the form "the team that invested most ended richest", is dominated by the cohort's draw. Likelihood: every session. Detect: never from one cohort.
**Fix (none needed pre-class).** **Mitigation.** Debrief and rank on M_R, reputation and SLO; show treasury beside the cohort's `black_swan_events`; do not compare treasury across cohorts.
**Passes.** (b) executable Monte-Carlo (34 + 14 + 5×2 games); (c) independent: BALANCE_REPORT_BASELINE.md §1b's non-monotonic CapEx ladder reproduces byte-identically at HEAD (`fin/capex_grid.py`). **Dismissal.** Checked that the harness used production toggles and a real cohort seed per game (it did; `dry_run` scorers, round-robin R1 map; a perfect-map control changed nothing material).

#### F-10 · The graded exercise's answer keys, the facilitator's per-round script and every tuning constant are readable with no credentials on the public URL — P1 · CONFIRMED · C4
**Evidence.** `next.config.mjs:22-31` rewrites `/api/:path*` to the backend, so every backend route is public. No-credential sweep of all 361 HTTP routes (`~/audit_scratch/acc/sweep_noauth.py`): 57 answer 200. Leaks (handlers with no `Depends` and no in-body auth): `admin_teleprompter.py:960/1011` `GET /api/admin/teleprompter[/{round}]` — R2 text quoted from the 200 body: "★ R2→R10 PAYOFF: … ≥80% matrix accuracy AND Option A → materiality_aligned (+0.10 M_R, ~$36M …)"; R1: "The $3M audit (Option B) … has 5× NPV return at Round 4"; `admin_router.py:1559` `/simulation-reference` (21 KB: per-round A/B/C treasury/CI/reputation/NCD/SLO deltas + flag graph + `terminal_valuation.mr_components`); `:10633` `/engine-tunables`; `:10857` `/scenario-presets`; `router.py:4839` `/api/simulations/stakeholder-map/master` (10 KB: each stakeholder's `correct_quadrant` and per-tactic `"correct": true/false` — the R1 answer key; not fetched by any frontend file); `:12413` `/flag-dependencies`; `:12797` `/side-tracks/blueprints`; `:8897` `/interventions/master`; `admin_teleprompter.py:1221` `/journey/r6-revelation` (the R6 twist + `backfire_probability: 0.4`); `:1187` `/pedagogical/debrief-protocol`. Separately, `admin_router.py:7176` `GET /api/admin/materiality-config` is REQUIRED by the R2 matrix (`DoubleMaterialityMatrix.js:411`) and returns per-issue `financial_impact`/`societal_impact` (= the correct quadrant) and `panel_recommendations` — the audit harness builds a perfect matrix from it. `AUDIT_Classroom50_Readiness_2026-08-13.md` §F02 flagged three of these as a Do-Before-Class blocker with the exact fix; it was never landed.
**Failure scenario.** One participant runs `curl https://<host>/api/admin/teleprompter` and shares it; every table knows the payoff of every option and the max reachable M_R. Likelihood: low-moderate in a senior room, catastrophic for graded validity if it happens. Detect: never.
**Fix.** One line per handler: `async def get_teleprompter(round_number: int, _g: None = Depends(require_facilitator)):` (import from `admin_shared`; `admin_teleprompter.py:25` already defines one) for the ten handlers above; for `materiality-config`, serve a player projection that strips `financial_impact`, `societal_impact`, `correct_quadrant`, `panel_recommendations` unless `get_facilitator_from_request(request)` — the pattern already used for `esg_profile_weights` in `admin_router.py:500-540`. **Mitigation.** Block the ten paths at the edge and confirm 401/404 from a logged-out browser; accept the R2 rubric as transparent if that is a deliberate teaching choice.
**Passes.** (a) static read of each handler's guard chain; (b) no-credential sweep with bodies dumped; (c) frontend grep showing the player cockpit needs none of the ten. **Dismissal.** Prior rulings checked: only `global-settings` was ruled public, and its rubric is now stripped.

#### F-11 · `analytics/player/{session_id}` trusts a forgeable header — any caller who knows a rival's session id and 4-character player id reads that rival's committed decisions — P1 (P0 edge if read before the rival's round closes) · CONFIRMED · C4
**Evidence.** `admin_analytics.py:621-624` `if _get_fac_role(request) == 'anonymous': owner = sess.get("player_id") or ""; if owner and request.headers.get("X-Player-Id", "") != owner: raise HTTPException(status_code=403, detail="Not your session")` — a plaintext header compare, not the signed player token that every `router.py` player route now requires (`_assert_player_owns_session`, `router.py:309`); any authenticated facilitator passes with no ownership check. Probe (`~/audit_scratch/acc/probe_analytics_player.py`): `GET /api/admin/analytics/player/{B.sid}` with header `X-Player-Id: B.pid`, no token, no cookie → 200, body `decision_impact:[{"round":1,"choice":"option_c","total_capex":8000000,…}]`; facilitator of another cohort → 200.
**Failure scenario.** Session ids and player ids leak through screen-share and the join flow; a participant reads a rival's committed choice and capex before committing their own. Likelihood: low; impact on a graded round: high.
**Fix.** Replace the block with `from router import _assert_player_owns_session; await _assert_player_owns_session(request, session_id, allow_observer=True)`. **Mitigation.** None without a deploy.
**Passes.** (a) static; (b) probe with a forged header and no token, and with an unrelated player's token (still 200 — the token is ignored). **Dismissal.** `tests/test_admin_route_guards.py:76` ratchet counts this inline check as "tenanted"; it is not.

#### F-12 · The CapEx term loan never reaches the balance sheet or P&L the participant sees — P1 · CONFIRMED · E1
**Evidence.** `balance_sheet.py:736-740` reads the loan from its `events` argument (`if "capex_loan_balance" in events: bs["non_current_liabilities"]["capex_term_loan"] = …`; `:783` interest from `events.get("loan_interest_payment", …)`), but `round_logic.py:731-741` builds a NEW dict with seven keys — `csf_this_round, total_capex_allocated, dividends_paid, remediation_events, tipping_tier, green_bond_issued, green_bond_amount` — and passes it at `:743`. No frontend row exists for the loan (`grep capex_term_loan frontend/app` → none; `SustainabilityBalancedScorecard.js:1181-1185` lists Revolving Credit / Green Bonds / Provisions / Decommissioning / Leases). Probe (`~/audit_scratch/fin/bs_loan_probe.py`): same state, engine event shape → `capex_term_loan=9,000,000 interest_expense=1,103,500 total_liabilities=54,155,000 D/E=0.76`; production `bs_events` shape → `capex_term_loan=0 interest_expense=647,500 total_liabilities=45,155,000 D/E=0.57`. 20 HTTP-driven runs: `bsLoanMax=0` in every run while `active_event_flags.capex_loan_balance` reached $26.9M. The launch-audit test (`tests/test_launch_audit_2026_09_01.py:1052-1059`) passes the engine dict straight to the function, bypassing the wiring.
**Failure scenario.** Any team whose round CapEx exceeds 20% of opening treasury (the cockpit pool) sees PPE up by the loan-funded CapEx with no matching liability; equity overstated by up to $27M; D/E understated; interest omitted. F-05 (the owner's design decision) exists to teach "the balance sheet shows the debt".
**Fix.** Add to `bs_events`: `"capex_loan_balance": events.get("capex_loan_balance", (global_state.get("active_event_flags") or {}).get("capex_loan_balance", 0.0)), "loan_interest_payment": events.get("loan_interest_payment", 0.0)`, and a "CapEx Term Loan" row in the two statement components. **Mitigation.** Say aloud: "the term loan is in the finale's net debt but not on the round statements" (the finale bridge does read it, `round_logic.py:2760-2765`).
**Passes.** (a) static; (b) probe with both event shapes + 20 runs; (d) the existing test shown to bypass the wiring. **Dismissal.** Searched for a second sync path (only the finale read); checked whether `events` in `run_new_engines` could be the engine dict (it is, but `bs_events` is rebuilt).

#### F-13 · A negative treasury is charged interest twice per round by independent code paths, plus a covenant surcharge — ≈22–33% per 6-month round; the second charge is invisible — P1 · CONFIRMED · E3/E6
**Evidence.** `engine.py:3357-3359` `if ctx.new_treasury < 0: debt_service = round(abs(ctx.new_treasury) * corporate_cost_of_capital, 2); ctx.new_treasury = round(ctx.new_treasury - debt_service, 2)` (per round, no /2). `balance_sheet.py:509-514` `rate = … loan_interest_rate … 0.12; interest = round(deficit * rate / 2.0, 2); … gs["corporate_treasury"] = round(gs["corporate_treasury"] - interest, 2)` on the same deficit; `check_covenants` (`balance_sheet.py:456-461`) adds `net_debt * 0.05/2` when breached. Probe trace (fin, legacy_abc/advanced R6): opening −19.26M → engine 1,773,347 + revolver 2,377,181 + covenant 2,240,492 = $6.39M of financing charges in one round (33% of the deficit); healthcare R10: $84.1M on −$377M (22%). No frontend consumer of `short_term_debt_interest_charged` (grep → none). 10 of 15 pure-A runs at $3M/BU ended negative.
**Failure scenario.** Once negative the deficit compounds at ~15%/round; recovery is arithmetically impossible within the remaining rounds; the player sees one of the two interest lines, so the debrief cannot explain the trajectory.
**Fix.** `balance_sheet.py:573` `_sweep_negative_cash(bs, gs, diagnostics, charge_interest=False)` (keep the reclassification, drop the second price), and apply `/2` consistently. **Mitigation.** Brief "a negative treasury costs ~15% per round"; use god-mode bailout tools if a team is stranded (not verified here).
**Passes.** (a) static read of both paths; (b) per-round trace + line-level attribution (`fin/line_trace.py`). **Dismissal.** Looked for a guard making the balance-sheet charge conditional (none — `charge_interest=True` unconditionally); checked FIX-A at `round_logic.py:750-753` (removed a duplicate covenant surcharge, not this).

#### F-14 · The Trading-Floor, the God-Mode leaderboard and the grading CSV rank teams on a proxy "Terminal Value" that is not the one awarded — P1 · CONFIRMED · E2/F3
**Evidence.** `admin_router.py:6113-6118` `# Terminal Value projection: Treasury + (Rev - OPEX) * Synergy * 5` / `terminal_value = round(gs.get("corporate_treasury", 0) + (total_revenue - total_opex) * synergy * 5, 2)` — never reads `flags["terminal_value"]`; sorts at :6277/:6285. The neighbouring endpoint already knows better: `:6504-6508` `authoritative_tv = flags.get("terminal_value")`. Consumers: `admin/trading-floor/page.js:66-70` (sorts on `terminal_value`; header: "ranked board of every team by Enterprise Value … Ring the Bell"), `LeaderboardMatrix.js:239`, `ReportsExport.js:193` (CSV "Terminal Value"). Probe (val, three completed games): served −$426.2M / −$82.0M / $122.5M vs awarded $0 / $59.6M / $279.9M; seam: alpha 182,714,797.72 served vs 611,730,457.77 awarded. The rows carry no `price_per_share`, so the trading-floor "IPO delta" (page.js:227-229) never renders.
**Failure scenario.** Team X: awarded TV $204M, projector $180M; Team Y: awarded $510M, projector $100M; the projector ranks X first, every player screen ranks Y first by 2.5×; sign flips too.
**Fix.** In `get_leaderboard`: `_tv = (gs.get("active_event_flags") or {}).get("terminal_value"); terminal_value = round(float(_tv), 2) if _tv is not None else <proxy>`; add `price_per_share`, `equity_value`, `archetype` to the row. **Mitigation.** Keep the Trading-Floor OFF; do not read the God-Mode "Terminal Value" column; grade from each team's scorecard / `final_report_canonical.terminal_value`.
**Passes.** (a) static; (b) HTTP probe as god_mode against completed games. **Dismissal.** Looked for a post-R10 override in this function (only in the other endpoint).

#### F-15 · Mid-game M_R estimates, the Consequence-DNA projection, the M_R Ladder and the Mirror Debrief compute M_R from the raw flag dict, so list-held flags are invisible — estimates diverge from the award by up to −0.57 and the "biggest missed lever" is told to teams that earned it — P1 · CONFIRMED · E2
**Evidence.** Finale input: `round_logic.py:2648` `_mr_flags = {f: True for f in all_flags}` with `_collect_all_flags` expanding `rN_flags` lists (:3292-3325). Preview: `engine.py:4762-4764` `_pv_flags = dict(current_global.get("active_event_flags", {}) or {})` / `_pv_flags.update({k: v for k, v in ctx.events.items() …})` / `_pv_mr = calculate_mr(` — and `hr_investment_rounds` (:4770) has no writer. `consequence_dna_api.py:321` and the What-If (`admin_router.py:12445-12459`, which also uses a fixed 12× multiple) do the same. Storage layout (probe): `"r7_flags": ["waste_to_energy", "synergy_unlock"]`, top-level `synergy_unlock: None`. Grid (`~/audit_scratch/val/probe_preview_vs_finale.py`, 384 states): 370 divergent; preview understates by up to 0.57 and overstates by 0.20 (Resilience granted despite `insurance_only`). Frontend second arbiters: `utils/mrJourney.js:36` `captured = !!flags[op.flag]`; `MirrorDebrief.js:116` `const earned = !!flags[opp.flag];` with its own table of eight flag names of which none is top-level for any probed player → the "biggest missed lever" is always `resilience_investment +0.20`, told to a player who earned `resilience_bonus 0.20`. Consumers: `RoundBriefing.js:286-288` "Preliminary valuation estimate" (rounds > 5), `ConsequenceDNAVisualizer.js:743-745`, MRLadderReveal/MirrorDebrief (default-on, `playerVisibilityRegistry.js:78-80`). `engine.py:4750-4756` comment claims "the same calculate_mr projection the finale uses" — false for flags.
**Failure scenario.** A team with Resist & Integrate (R7-C), Ethical AI (R6-B) and Community Fund (R9-C) sees an estimate built on M_R 0.80 while the award is 1.27; the Mirror Debrief tells them they missed the lever they took.
**Fix.** `from round_logic import _collect_all_flags; _pv_flags = {f: True for f in _collect_all_flags(...)}` at `engine.py:4762`, `consequence_dna_api.py:321`, `admin_router.py:12445` (+ `use_dynamic_multiple=True`); render ladder/mirror from `flags.mr_breakdown` (`round_logic.py:2903`). **Mitigation.** Disable `mirror_debrief`, `mr_ladder_reveal`, `rewind_ribbon`; brief that the mid-game estimate ignores strategic flags.
**Passes.** (a) static read of all call sites; (b) full games + 384-point grid; (c) `tests/test_mr_single_arbiter.py` uses `{}` flags so cannot catch it. **Dismissal.** No writer stores these flags as top-level booleans (only `materiality_*`).

#### F-16 · The F4 "patience clock" forces NPC hostility on every team on a timer — −15…−20 group reputation for a well-run team, periodic protests, and near the jittered boundary a $5–25M regulator fine — none of it visible or attributable — P1 · CONFIRMED · E3/E6
**Evidence.** `npc_stakeholders.py:546-557` `# Patience clock (F4) …` `if (tier_idx >= 1 and npc_state["rounds_at_tier"] >= patience_limit and tier_idx < len(escalation_levels) - 1): tier_idx += 1 …`; `config.py:434` `PATIENCE_LIMIT … 3`; enabled in production `pedagogical_engine.py:728` `"stakeholder_uncertainty_enabled": True`. Tier 0 needs satisfaction/trust ≥70 for all four NPCs (:220/268/316/363); at an "excellent" state (rep 85, SLO 85, CI 20, gov 5) three of four are still tier 1 (`~/audit_scratch/soc/p11.log`). Consequence: hostile/adversarial/enforcement/legal actions cost −5/−3 group reputation (:682-685); regulator enforcement → the fine (:691-694). Probe (`p10`, production chain, even 100%-of-pool team, 12 seeds): F4 ON → mean 3.5 hostile actions, −16.8 reputation, R10 reputation 62.6; F4 OFF → 0 hostile actions, R10 reputation 79.7; identical SLO. Fine vectors identical across opposite strategies in 9/12 seeds (`p8c`). No player surface renders `npc_actions`/`regulatory_fine` (F6 Intel Rail default OFF, `pedagogical_engine.py:729`); no facilitator view either.
**Failure scenario.** A team spends its whole pool evenly for 10 rounds (SLO 55 → 83) and at R3, R6, R9 is hit by "Community Protest" + "Public Campaign" (−5 each) and a "Formal Investigation"; nothing on screen names the cause; the debrief cannot trace it to a decision. The lesson the code teaches a good team is "stakeholders turn hostile on a timer".
**Fix.** `npc_stakeholders.py:552` `tier_idx >= 1` → `tier_idx >= 2` (arm the clock only from the first hostile-ish tier; re-fixture `test_patience_forces_escalation_and_cooperative_resets`). **Mitigation.** Per cohort switch OFF "🎲 Uncertain Thresholds" (`stakeholder_uncertainty_enabled`) — removes every forced action while metric-driven escalation stays.
**Passes.** (b) executable probes p7/p8c/p8d/p10/p11; (a) static read of `determine_npc_action`/`process_npc_tick`. **Dismissal.** Checked the staged-escalation limiter (:531-545, caps +1/round — the clock's step is that one step), F1 trust asymmetry (keeps the tier sticky), and the agents' clock (arms only from "watching", dormant reachable — fine).

#### F-17 · Natural-decay tiers are evaluated on the GROUP-AVERAGE investment ratio, not the BU's own; the 09-03 calibration measured a quantity the engine never evaluates — P1 · CONFIRMED · E3/E6
**Evidence.** `engine.py:3089` `_inv_ratio = ctx.events.get("_pre_austerity_avg_invest", dec.get("investment_ratio", 0))`; `:4632-4634` `_pre_austerity_avg_invest = sum(d.get("investment_ratio", 0) for d in decisions) / max(len(decisions), 1)`; `:4702` `ctx.events["_pre_austerity_avg_invest"] = _pre_austerity_avg_invest` — always set, so the per-BU fallback is dead and `:3094-3096` pass the same ratio to every BU (only `capex_abs` is per BU). The golden trace itself shows it: `tests/golden/stakeholder_slo_trace.json` R1 SLO 58.12 for pharma (ratio 0.7, $1.5M) AND electronics (ratio 0.05, $0), while the fixture comment (`tests/test_stakeholder_golden_trace.py:143-144`) says the ratios were "chosen to straddle the SLO decay/growth thresholds". Probe (`~/audit_scratch/soc/p2.log`): pharma $2.5M (ratio 0.5) + three $1 BUs → avg 0.125 → pharma flat, tokens decay; golden ratios → +3.12 for all four incl. $0 BUs. `config.py:589-592` "no_decay_ratio is structurally INERT given the $100k floor" is false under engine semantics (the average shields $1 BUs). The three 09-03 commits and `DELTA_natural_decay_recalibration_2026-09-03.md:29-35` reason per decision ("169 of 241 real decisions").
**Failure scenario.** A team puts $2.5M into Electronics to repair its licence and $1 elsewhere: Electronics is flat, the three untouched BUs lose 4%/round; the cockpit's per-BU SLO teaches "investing in a BU does nothing for its licence".
**Fix.** `engine.py:3089` → `_inv_ratio = float(dec.get("investment_ratio", 0) or 0)` with the austerity fallback only when `cfo_austerity_active`; rebaseline both golden traces in the same commit; re-measure the 241 decisions per BU before touching the bands again. **Mitigation.** Facilitator states that SLO growth is driven by the company-wide average ratio and by ≥$3M in a single BU.
**Passes.** (a) static (re-read by the lead); (d) golden-trace JSON; (b) single-round probe. **Dismissal.** Looked for a per-BU path (only writer is :3096), for austerity-conditionality on the average (:4702 unconditional), and at the router recompute (:2729-2746 — per-BU ratios are correct on entry; the averaging is engine-side).

#### F-18 · Pacing: the wizard's pacing choice is never enforced; the default free-mode barrier is client-only; the briefing-screen clock polls the wrong session — P1 · CONFIRMED · F2/F4/F1
**Evidence.** (i) `CreateCohortModal.js:727-733` PUTs `/api/admin/cohort/{sid}/pacing` with `pacing_mode` `free_play|manual|scheduled`; `admin_router.py:10985` `pacing["mode"] = body.get("pacing_mode", pacing.get("mode", "free"))` — `unlocked_round` stays 999 and `max_unlocked_round` is stored but read by nothing; gate `admin_shared.py:1527-1534` `if pacing["mode"] == "free": return True … return round_number <= _effective_unlocked_round(pacing)`. Probe (`~/audit_scratch/flow/probe_wizard_pacing.py`): `[manual] … max_unlocked_round said 1` → player commits R1, R2, R3 `201 201 201`; RunBar "Advance" → 422 "target_round must be between 1 and 10" (`RunBar.js:104` computes `999 + 1`); `RoundPacingControl.js:115` `setMode('free_play')` matches no mode card, hiding "Force Advance". (ii) Default `mode: free, unlocked_round: 999, free_advance_timeout_seconds: 0` (`admin_shared.py:1453-1468`); the "Waiting for Other Teams" barrier is `cohortWaiting` in React state only (`ExecutiveCockpit.js:722-726`; results screen :2951-2957 has no button); a reload skips it and the server accepts R2 while a sibling is on R1 (`probe_flow.py` A3 → 201). Force Advance releases it and auto-commits laggards from the draft or "Option B, $1 per BU" (`router.py:528-540, 599-604`), disclosed on the amber card but the transient banner (`page.js:1798`) says "Time expired" even after a facilitator Force Advance. (iii) `DecisionPressureTimer.js:49` polls `/api/admin/sessions/{PLAYER sid}/pacing` — the endpoint creates a fresh default for the sub-session (`admin_router.py:3632-3635`) → "No time limit this round" on the briefing screen while the cohort countdown runs (`probe_pacing_scope.py`).
**Failure scenario.** Facilitator picks "Manual" in the wizard and briefs; fast tables commit R1–R3; the facilitator's Unlock button fails with "target_round must be between 1 and 10". In default free mode one absent team holds 49 on "Waiting for Other Teams" until Force Advance. Likelihood: high. Detect: minutes. Recover: Round Pacing → Manual → Apply (2 clicks) / Force Advance.
**Fix.** In `save_cohort_pacing` map `free_play→free`, `manual→manual` + clamp `unlocked_round` as `set_pacing` does (`admin_router.py:3810-3814`); server-side barrier in free mode; resolve `parent_cohort_id` in the timer as `CountdownTimer.js:45-56` does. **Mitigation.** After creating the cohort, Round Timeline → Round Pacing → **Manual** → Apply (enforced path verified: locked commit → 403 "Round N is locked…", `router.py:2568-2571`); set a per-round limit in free mode; brief "do not reload after committing".
**Passes.** (a) static read of wizard → endpoint → gate; (b) HTTP probes of all three modes and of run-ahead. **Dismissal.** Checked whether the wizard also calls the live pacing endpoint (no) and whether `pace-lock` is set by any frontend (no caller).

#### F-19 · Four display engines assert numbers their construction cannot support, and the UI presents them as consequential — P1 · CONFIRMED · E6
(a) **Biodiversity**: `round_logic.py:692` `"natural_capital_debt_delta": events.get("natural_capital_debt_delta", 0)` — no writer of that events key exists (only `natural_capital_debt_applied_rN`); `biodiversity_engine.py:81` `deforest_impact = deforestation_rate * 2.0` (constant 2.5, no writer), `:94` `natural_drift = -1.5` → EHI −6.25/round unconditionally: 65 → 2.5 by R10 for every team; "Nature's Invoice" charged to treasury every round from R2 (:465-468); rendered red `< 40` (`SystemEngineMetrics.js:24-28`). Full-game harness: the greenest and the extractive bot produce the identical EHI series. (b) **TCFD**: `tcfd_scenarios.py:164` `total_emissions_proxy = avg_ci * len(bus) * 1000  # Simplified tCO2e` assumes $1B revenue per BU (BUs are ~$13M) → "annual carbon cost" $45.8M = 85.5% of revenue (orderly 1.5°) on a session whose engine emissions are 2,635 t (`TCFDScenarioDashboard.js:347, 402`; DebriefReport 'tcfd' tab). (c) **Meadows**: `router.py:7048` `decision_history = gs.get("decision_history", [])` — no writer anywhere; `consequence_dna_api.py:537` calls `analyse_session_leverage_points(decision_history)` with the wrong signature, TypeError swallowed → "LP12 · 0% · low-leverage" for every team on the CEO interview and "System Effectiveness 0%". (d) **SDG**: engine group score vs router-recomputed `bu_scores` (`router.py:3598-3620`) in one payload — probe: `group_sdg_score` 65.75 with an alert "SDG 12 is 24 points below alignment threshold" while the engine's own SDG-12 for that BU is 99.98; four of eight inputs read wrong keys (`sdg_linkage_engine.py:133, 198, 205, 208`).
**Failure scenario.** The greenest team sees "Ecosystem Health 15.0/100" in red at R8 and pays for it; the TCFD tab tells a $53M-revenue group its carbon bill is $46–64M a year; every CEO interview says the team operated at the lowest leverage point.
**Fix.** (a) sum `natural_capital_debt_applied_r*` into the delta and zero the constant drift; (b) `total_emissions_proxy = Σ CI × revenue_base / 1e6` and index the per-decade price by `years // 10`; (c) pass `(decision_history, gs, bus)` and build the history via the DNA extractor; (d) router reuses `sdg_result["bu_scores"]`. **Mitigation.** Per cohort: `biodiversity_engine_enabled: false`, `tcfd_scenarios_enabled: false`; hide the SDG radar dock and the Meadows card.
**Passes.** (a) static wire traces; (b) probes (`~/audit_scratch/imp/probe_bio.py`, `probe_tcfd.py`, `probe_meadows.py`, `probe_sdg.py`) + full-game harness. **Dismissal.** Searched writers of the dead keys and for frontend rescaling (none).

#### F-20 · The reveal's share price is on a scale that makes every team "red", and its "Natural Capital Debt" is an index rendered as dollars and subtracted from dollars — P1 · CONFIRMED · E3/E6
**Evidence.** `simulation_config.json` `terminal_valuation.shares_outstanding = 100000000`, `ipo_price_per_share = 50.0` (`config.py:772-773`) → implied IPO market cap $5.0B; observed terminal EVs $0–$0.9B → `price_per_share` $1.00 (floor) to $3.13; best bot ≈ $8.9; colour ladder `GameOverSummary.js:138-139` green `>= 50`, amber `>= 30`, else red — even the theoretical maximum (~$1.0B EV) is $10/share. The in-game ticker starts at the same $50 (`stockValuationEngine.js:16-17`) so ten rounds of a $35–60 ticker end in "Share Price $3.02 — loss −94%" for a solvent team. NCD: `config.py:213` `NCD_HARD_CAP … 5_000` (an index; observed R10 sums 47.0 and 92.2); `ArchetypeReveal.js:374-379` `adjustedFinalValue = mrAdjustedValue - total_ncd`, `:628-641` `value={-total_ncd}` with `formatter={(v) => fmtCurrency(v)}` under "Natural Capital Debt (NCD) — Accumulated ecological liability"; backend gate `round_logic.py:2798-2801` `_dmav = gs.get("corporate_treasury", 0.0) * mr - sum(b.get("natural_capital_debt", 0) …)` — the NCD term is inert against dollars.
**Failure scenario.** "$63.0M → ×0.97 → $61.1M − $47 = $61.1M" on the screen meant to teach double materiality; every team's share price is red.
**Fix.** `shares_outstanding` 6,500,000 in the volume config (and `stockValuationEngine.js:17`) → EV $280M ≈ $43 (amber), $891M ≈ $137 (green); `ArchetypeReveal.js:641` formatter → `${v.toFixed(0)} NCD pts`; scale or drop the NCD term in `_dmav`. **Mitigation.** Hide the Share Price stat; say the NCD line is an index.
**Passes.** (a) static read of config → bridge → three consumers; (b) seven full games + config print. **Dismissal.** Looked for a rescale on the volume config (identical), in the frontend (`toFixed(2)` on the raw value), or an EV-scaled share count (fixed constant).

---

## Flow walkthroughs (Q2)

Both narratives are for `legacy_abc` cohort play on HEAD; evidence is file:line or a probe in `~/audit_scratch/flow/`. Tags: **[STUCK]** no forward action and no self-recovery; **[CONFUSING]** decidable but misleading; **[DEAD-END]** intended stop with no visible path out.

### Participant
1. **Sign in** (`JoinCohortModal.js:154-174`): Team ID + password; copy promises "you will be asked to set your own" (:243). Credential `MUR-XXXX` / `MUR-XXXX@123` (`default_credentials.py:8`). Login stores session id, player id, signed token and ws ticket in localStorage (`useSimulation.js:418-431`); a second tab or a reload re-uses them. **[STUCK — F-01]** the promised password change never appears; every write from here on is 403.
2. **Username prompt** (`page.js:1371-1389`) — passes on the default password (probe: 200).
3. **Round briefing** (`page.js:1395-1423`): separate screen. "📋 Last Round Summary" is empty from R2 on — `RoundBriefing.js:404-430` reads `.choice/.treasury_delta` but history rows carry `choice_selected/global_state` **[CONFUSING — FLOW-06]**. The pacing chip says "No time limit this round" whatever the cohort pacing **[CONFUSING — F-18(iii)]**. R1 only: a 7-step tour, persisted per session.
4. **R1 gate — Stakeholder Map** ("Before making strategic decisions, you must map your key stakeholders", `ExecutiveCockpit.js:2071-2079`; ≥6 placements, 2 attempts). Client-only gate (a direct commit without it → 201, FLOW-09); failing the quiz is not a dead end — accuracy only scales the R4 crisis (`round_logic.py:254-259`). With the default password: "Server returned incomplete data. Please retry." **[STUCK — F-01]**.
5. **Option A/B/C, then allocation** across BUs against the CSF pool = max(20% treasury, $5M) (`page.js:398-410`; server recomputes ratios `router.py:2742-2746`). Over-allocation shows "⚠️ Over-allocated" and the CTA becomes "Reduce your allocation to commit" (`ExecutiveCockpit.js:2447-2449, 4049`) — the server accepts any total, caps at 2× treasury and loans the excess (probe D1 → 201). Labels/units: the pool is called "CSF" in the cockpit and "investment ratio (CAPEX/Revenue)" in the glossary (`TechnicalGlossary.js:75`) — it is CAPEX / pool **[CONFUSING]**.
6. **Commit**: single CTA → preflight gates with explicit "🚨 Action Blocked" copy (`page.js:1303-1339`) → "Predict Before You Commit" → "✓ Commit decisions" → ceremony "Processing Decision…" bound to the request. Double-click: client `commitInProgressRef` (`useSimulation.js:466-470`); server per-session lock 409 (`router.py:2334-2339`), 5-s cooldown 429 (:2410-2417), `expected_round` 409 `stale_round` (:2531-2542) → silent client resync. Verified by probe B1/B2 and `ops/probe_commit_race.py` (`[201, 409]`, exactly one advance).
7. **Results** overlay: "⏩ Advance" or "Waiting for Other Teams X/Y committed" with no button (`ExecutiveCockpit.js:2951-2957`) **[DEAD-END-ish — F-18(ii)]**: released by all-commit / timeout / Force Advance. "The Road Not Taken" shows identical gains for both alternatives **[CONFUSING — F-06]**; "reputation rose N points" can be wrong when reputation hit 0 **[F-03]**. Reload here → briefing of N+1 with no recap **[CONFUSING — FLOW-06]** and, in free mode, the ability to run ahead.
8. **Locked round (Manual/Timed)**: "🔒 Round Locked — … Your decisions are saved. The facilitator will advance the round." (`ExecutiveCockpit.js:3963-3980`); lifts within one 5-s poll or a WS push. Direct POST while locked → 403 "Round N is locked…" (`router.py:2568-2571`) → overlay "Waiting for Facilitator" with Dismiss.
9. **R2 gate — CSRD matrix** (`page.js:1841-1955`, POST `/materiality`; CFO gate 400 → "Force Override"). Empty submission accepted (probe → 200, budget 0). The matrix's issue data (`/api/admin/materiality-config`) carries the correct quadrants **[F-10]**.
10. **Connection drop (90 s)**: banner after two failed 5-s polls ("Reconnecting to the server — the board may be out of date", `ConnectionBanner.js:50`); WS reconnects every 5 s; a lost commit response → toast "⚠️ Failed to fetch" → retry → 429 → auto-retry → 409 `stale_round` → silent resync to N+1 (no results screen). Drafts survive only if autosave fired: it runs 30 s after the last slider change and only once an option is chosen (`page.js:847-856`) **[FLOW-05]**; Force Advance then commits defaults (Option B, $1/BU) for a team whose draft never saved.
11. **Two tabs**: the second tab jumps to the server round within 5 s (`useSimulation.js:762-776`); concurrent commits → 409 "Another commit is in progress" toast.
12. **Round 10 → ending**: `gameOver` when `active_event_flags.profile` arrives (`useSimulation.js:313-330, 613-615`) → ArchetypeReveal → Scorecard → BoardroomShowdown → GameOverSummary. The live reveal renders the R10-entry state; a reload renders the final state **[F-08]**; share price red for everyone, NCD shown as dollars **[F-20]**; the Mirror Debrief names a "missed lever" the team took **[F-15]**; the Front Page's closing treasury and counterfactual table are wrong **[F-06, F-08]**. The archetype label, M_R value and terminal value are consistent across the six surfaces that show them (verified-good).
13. **Day 2 of a split session**: player token 12 h, no refresh; re-login with the personal password → fresh token bound to the same session; `must_change_password` stays false (ACC §D probe). A facilitator "reset password" re-arms the first-login gate and locks the team out until they run the "Forgot your password?" recipe (FLOW-07).

### Facilitator
Login `/admin` (FAC-NNN / FAC-NNN@321, `default_credentials.py:9`; the forced change modal IS rendered for facilitators, `admin/facilitator/page.js:1375-1381`). Entry tab Dashboard Home; RunBar on every tab once a cohort is selected (round, X/Y committed, mode, pacing, projector, health dot — the dot is green on any `/health` 200 and never checks the database, OPS-4).

| Action | Clicks | UI location | Endpoint + guard | Documented where | Verdict |
|---|---|---|---|---|---|
| Create cohort (paradigm, tier, industry, teams, ending pathway, seed, pacing) | wizard | Dashboard Home → CreateCohortModal | `POST /api/simulations/start` (+ `PUT /cohort/{sid}/pacing`) | tooltips only | pacing step is a no-op (F-18); `rng_seed` never reaches live state (RNG-2; fairness unaffected) |
| Issue credentials | 1 per player / Bulk Upload | Player Registry (`PlayerRegistry.js:195-231`) | `POST /{sid}/generate-player` (`require_sim_manager`) | none in *.md; password shown on screen + Copy | server enforces the change; UI never presents it (F-01) |
| Reset a credential | 2 | Player Registry (:263-299) | `POST /players/{id}/reset-password` (`require_sim_manager`; no cohort-ownership check, ACC-4) | none | resets to `ID@123`, copy says "random", re-arms the gate (FLOW-07) |
| See who committed / who is stuck | 0 / 1 tab | RunBar tally; per-team ✓/◐/○ in Cohort Pulse under Analytics (15-s poll) | `GET /cohort-pulse/{id}` | tooltip | usable; per-team view two tabs away |
| Set pacing / extend a round | 2–3 | Round Timeline → Round Pacing → mode card → Apply | `POST /sessions/{id}/pacing` (`require_sim_manager` + ownership; interval clamped 180–1800 s) | none | works; wizard cohorts show no active mode until re-applied (F-18); Timed is single-shot — re-arm each round |
| Unlock next round (Manual) | 2 | RunBar "⏭ Advance" or Round Pacing "Unlock" | `POST /pacing/unlock` | none | RunBar variant is the non-idempotent bare call (FLOW-11); Round Pacing sends `target_round` (idempotent, verified) |
| Undo a mis-unlock | 1 within 60 s | Round Pacing relock window | `POST /pacing/relock`; 409 once anyone committed (verified) | none | good |
| Unstick / advance stragglers (free mode) | 2 | Round Pacing → Free Play panel → Force Advance | `POST /force-advance` (ownership) | none | works; auto-commits from draft else defaults, exactly once (verified); hidden for wizard cohorts until Free Play re-applied |
| Undo one team's last round | 2–3 | Live Classroom → Undo Round (player session selected) | `POST /{sid}/undo-round` (`require_lead_facilitator`) | tooltip | works (probe: 200, team re-commits 201); base facilitator cannot |
| Roll back the whole cohort | — | same, cohort selected | `?cohort_wide=true` | tooltip promises it | button permanently disabled for cohort shells; API call rolls children back then returns 400 (OPS-2) |
| Pause the cohort | 1 | Cohort settings (god-mode UI) | `PATCH cohort-settings {system_frozen}` | none | commits → 503 with the freeze message; survives restart; players see "server busy" (FLOW-10) |
| Watch as a player | 1 | Live Classroom → Team Impersonation | player routes with the facilitator cookie | tooltip | ok |
| Export | 1 | God Mode → Backup & Export; Export Reports (CSV) | `GET /god/export` (`require_super_admin`); per-team `export-my-data`; CSV | — | CSV "Terminal Value" is the proxy (F-14); no cohort-level export for a base facilitator |
| Recover a crashed cohort | n/a | — | pacing derived from `next_unlock_at`/`schedule` after restart (`admin_shared.py:1484-1524`) | DEPLOYMENT_CHECKLIST | timed unlock survives restart; auto-commit timer does not re-arm |
| Debrief | 1 tab each | Leaderboard, Round Debrief, Debrief Narrative, Trading Floor, Front Page | — | tooltips | Round Debrief cards wrong decision (F-02); narrative reputation (F-03); Trading Floor proxy TV (F-14); Front Page P&L (F-08); admin leaderboard treasury/reputation/SLO/M_R consistent (verified) |

None of Force Advance, Round Pacing, Relock, Undo Round, the `@123` default or "Forgot your password?" appears in README.md, CLAUDE.md, SETUP_STEPS.md or DEPLOYMENT_CHECKLIST.md (grep counts 0); the facilitator manuals are .docx (text-extracted for the runbook check only, OPS-7).

### Pacing realism (F4)
Engine cost ≈2.4 s per commit in this environment; a Force Advance of two laggards took 6.2 s inside a player's dashboard poll (`ops/probe_force_advance_race.py`, `flow/probe_advance.py`) — twenty absent teams would take ~50 s inside one player's poll (PLAUSIBLE; settle with a 20-team probe). Per round the participant faces: briefing + option + 4 allocations + prediction + commit + results; R1 adds the tour and the ≥6-placement map (2 attempts); R2 adds the matrix; advanced-climate adds a mandatory module overlay each round R3–R10. No measured per-round time exists in the repo. Ten rounds in 2–4 h with 50 people is plausible only with **Manual** pacing and the facilitator driving unlocks, and only if F-01 is fixed or drilled — otherwise the first 20 minutes are lost to password recovery.

---

## Verified-good (checked and cleared, with evidence)

**Engines**
- Money conservation inside `process_tick`: the repo's own `_residual` law returns 0.00 every round on all five `VALID_DECISION_PARADIGMS` × 3 compositions, including `brsr_ngrbc` which the test omits (`fin/law_all_paradigms.py`); A = L + E to the cent in 200/200 statements; PP&E and working-capital roll-forwards exact; response treasury == stored treasury every round. The earlier "$656M gap" was the pre-fix ratchet ceiling in `test_treasury_waterfall.py:15-20`; the harness now asserts 0 and passes (61 tests).
- Transient OPEX surcharges (B-2) reverse to the cent in round N+1 (`fin/surcharge_transient_probe.py`, `engine.py:2519-2553 rescale_bu_transients`).
- CapEx term-loan arithmetic: repayment = opening/rounds_remaining, interest = opening × 12%, R10 bullet clears the balance (probe: R6–R10 repayment constant 4,027,865 = 20,139,324/5); `TestF05CapexFinancing` passes.
- Division guards on every ratio inspected: CAROIC (`engine.py:2194-2197`), covenant ratio (`balance_sheet.py:428-432`), liquidity (:885), D/E (:867-870), brain-drain (`engine.py:3211`); every treasury write is `round(…, 2)`; floor clamp evented and waterfalled where it fires.
- One M_R arbiter at the finale for all paradigms (`round_logic.py:2613, 2649`; BRSR `:412-413`); AST tripwire `test_every_finale_path_routes_through_calculate_mr` with an empty exception list; `tests/test_mr_single_arbiter.py` 52 passed. Clamp [0, 2.05] and ceilings 1.93/2.02 executed and reachable. DEEP-2/3 (inline cliffs, no clamp) are closed at the finale. Archetype label, M_R and TV agree across dashboard, final-report, front-page, DNA snapshot and canonical (seam probe, 6 surfaces).
- Gordon exit multiple 11.53–13.30× at WACC 9.7–10.9%, inside the 6–18 clamp and MODEL_CARD's sector ranges; EBITDA floored so TV ≥ 0; price floors at $1 with `equity_wiped_out`.
- All engine-side stochastic draws seeded from stored flags via `event_rng`/`event_seed` (black swans `black_swan_registry.py:673`, shocks `systemic_risk_engine.py:417`, overrun `engine.py:2899-2904`, FX :2668-2671, macro :2729-2732, carbon offset :3712-3714, fog :4082-4087, R5/R9 rolls `impact_engine.py:114/192/328/367`); global reseed (B-1) gone (`grep 'random\.seed('` → tests/harness only); two identical teams → 0 field diffs over 10 rounds with the NPC draw stubbed; `test_seeded_stochastics.py` 8 passed, `test_cohort_seed.py` 10 passed; option shuffle deterministic per session and deshuffled at commit.
- The EU AI Act deferred charge fires: `_apply_eu_ai_act_liability` runs from `post_tick` every round (`round_logic.py:554-556, 1863-1899`); probe R7 `eu_ai_act_compliance {cost: 3000000, governance_risk_delta: 5}`, R8–R10 `eu_ai_act_monitoring {cost: 1000000}` in both paradigms — the prior lead is refuted.
- Kept promises: `early_decarboniser` → R7 +0.10 synergy; `synergy_unlock` +0.15 M_R; `community_fund` +0.18; `insurance_only`/`electronics_water_priority` block +0.20; R2 tier → R3 green-bond and R10 +0.10/+0.05; R1 flags → R4 severity 40/80/60; R8-C NCD −30 matures at R10; DSO deferrals mature every round.
- Golden traces: `test_stakeholder_golden_trace.py` + `test_financial_golden_trace.py` 20 passed on HEAD; both golden JSONs byte-identical to the pre-item-4 commit (`git diff --stat b1d0dd4 HEAD -- backend/tests/golden/*` empty) — 278365d's claim holds. Shipped natural-decay constants printed at runtime: 0.96 / 0.15 / 0.20 / 0.30 / $100k / $3M / 1.0.
- SLO/sentiment/trust clamps enforced at every writer read (engine 3427/4396; round_logic 948-949, 1361-1362, 1459, 1799-1800, 2061, 2282, 2398-2399; NPC/agents/engagement/sentiment).
- ESG WACC wired, right direction, clamped [3%, 20%] at CI 1e9; tipping irreversibility pinned by 33 tests; R9 friction live and bounded; R5 cyclone seeded with a 0.95 threshold cap; turnaround direction (trust before cash) holds, 4 calibration tests pass.

**Config and deployment**
- Seed-once-then-volume-wins implemented as documented (`runtime_paths.py:263-282`); unwritable volume falls back to the in-image copy; the test suite runs on the committed config.
- Clamps fire and say so: probe on a stale volume printed `[CONFIG] WARNING: engine_parameters.imitation_decay.default_rate=0.1 exceeds the 0.08 sanity ceiling … using 0.05`.
- Config reaches the formula: `calc_caroic(1M,1M,0) → 0.75` ⇒ tax 0.25; R3 `advanced_climate` `cbam_surcharge_applied = 298,858.76` on 2,989 t ⇒ $99.99/t.
- `scripts/patch_volume_config.py` correct and idempotent on a copy of the pre-refresh dev volume (7 keys, backup, second run "already correct").
- Boot refusals are loud: no `JWT_SECRET` in prod → `SEC-6 FATAL`, exit 1 (`main.py:148-157`); memory store in prod → `SEC-2 FATAL` (`:57-83`); secrets gitignored and dockerignored; single worker forced unless `MURESSONS_MULTIWORKER_VERIFIED`.
- Every string DEPLOYMENT_CHECKLIST.md asks the operator to look for exists in code (`[storage] data dir:`, `RAILWAY DETECTED WITHOUT A DURABLE DATA DIRECTORY`, `durable_storage`, `?strict=1` → 503, `[preflight] Deployment configuration looks correct.`, CI job names).

**Concurrency, durability, access**
- Same-session concurrent commits: exactly one advances (`router.py:2334-2340`; `ops/probe_commit_race.py` `[201, 409]`; `test_concurrent_commit_race.py` passes). Sequential double-submit: 201 then 429; the client's `expected_round` retry → 409 `stale_round` with "Your decisions were saved — nothing was lost." Late commit after a facilitator advance → the same readable 409. Force Advance is single-advance under concurrent polls with injected latency (`RESULT [2, 2, 2, 2, 2, 2]`).
- Postgres commit is one transaction (`database.py:1297 async with conn.transaction():`), `uq_session_round` → 409, immutability trigger blocks edits to history, migrations additive/idempotent — static, not run (`PG_PARITY=1 DATABASE_URL=… pytest tests/test_postgres_parity.py`).
- Memory snapshot atomic (tmp → backup → `replace`, `database_memory.py:217-232`); restart probe: round, treasury, saved draft, manual pacing, cohort freeze, registry entry and a pre-restart player token all survived.
- Pacing policy persisted in both stores and re-derived from wall-clock after restart; `session_reaper` reaps only expired solo sessions or cohorts with `data_retention_days > 0` (default 0) — an idle overnight cohort is untouched; cohort `end_date` never locks cohort players out.
- Rate limits: keyed per identity AND per IP with a 60-s window and 60-s auto-expiring bans (`rate_limit.py:22-35, 189, 227`); login 10/min per id, 300/min per IP → 50 participants behind one NAT do not trip it; commit cooldown 1 per 5 s per session; nothing else limited; polling ≈50 req/s at 50 players costs ~15–20% of one core in memory mode.
- Player realm fail-closed: `_assert_player_owns_session` on all 28 session-scoped player routes (AST scan); cross-player/cross-cohort → 403 on 31 routes × 6 actors; WebSockets require a facilitator JWT (`/ws/admin`) or a session-bound ticket (`/ws/session/{id}`), wrong session → 4001; participant → any state-changing admin route → 401 (279 routes probed, 0 write 2xx); `project_admin` 403 on every live-run endpoint; `PLAYER_MASTER_PASSWORD` disabled when unset; FAC-EMERGENCY password random and not logged in prod; `esg_profile_weights` now stripped from `global-settings`; login limiter does not lock a campus out; player token bound to its session (cohort-X token vs Y → 403); day-2 re-login works.
- Materiality resubmit is a no-op; join ×2 returns the same session; learning-bonus second claim awards 0; facilitator freeze stops commits with a readable message and survives restart.

---

## Coverage and gaps

**Swept exhaustively (read end to end or scripted over the whole set).** `engine.py` financial layer 2781–3416, all 14 `ctx.new_treasury` write sites, `process_tick` 4579–4797, `_assemble_global_state`; `balance_sheet.py` (all); `round_logic.py` `run_new_engines` 655–1240, `post_tick` entry, every `_post_rN` handler, EU-AI-Act, pulse revert, helpers, `_post_r10_grand_finale` 2293–3069, BRSR finale 300–560; `terminal_valuation.py` (all); `ending_pathways.py` 1–90, 546–790; `rng_util.py` (all) and every stochastic call site in backend (fresh grep, 21 files opened); `flag_taxonomy.py`; `config.py` (all 858 lines), `runtime_paths.py`, `config_introspect.py`, `config_excel.py` import path, `deploy_preflight.py`, `scale_preflight.py`, `scripts/patch_volume_config.py`, Dockerfile, `docker-start.sh`, `railway.json`, `next.config.mjs`; `npc_stakeholders.py` (trust, satisfaction, `determine_npc_action`, `process_npc_tick`), `autonomous_agents.py` (profiles, tick loop, patience), `org_politics.py` (all), `stakeholder_engagement.py`, `stakeholder_sentiment.py`; `impact_engine.py`, `biodiversity_engine.py`, `sdg_linkage_engine.py`, `systemic_risk_engine.py` (§1–5, 6b, 7), `meadows_leverage.py`, `turnaround_engine.py`, `tcfd_scenarios.py` 28–281, `black_swan_registry.py` 560–869; `rate_limit.py`, `session_reaper.py` (all); `router.py` commit path 2312–3370, pacing/free-advance 470–700, login 940–1075, ownership 309–421, `get_dashboard`/final-report/front-page/peer endpoints; `admin_router.py` pacing 3328–3960, `save_cohort_pacing`, reset-password, generate-player, undo-round, leaderboard 6045–6293, debrief 8454–8600, config upload/current 9055–9331, engine-tunables; `admin_analytics.py` 593–830; `admin_debrief_narrative.py` (all); `database_memory.py` snapshot/fetch/insert paths; `database.py` 95–430, 1274–1410; `main.py` 40–330, 552–696; `auth_jwt.py`; unauthenticated sweep of all 361 HTTP routes + 2 WS; player isolation 28 routes AST + 31 probed × 6 actors; admin reach 279 routes × 4 actors; frontend: `page.js`, `hooks/useSimulation.js`, `JoinCohortModal.js`, `RoundPacingControl.js`, `RunBar.js`, `DecisionPressureTimer.js`, `CountdownTimer.js`, `UndoRound.js`, `sidebarConfig.js`, `DebriefNarrative.js`, `DebriefReport.js` round cards, `GameOverSummary.js`, `FrontPageReveal.js`, `ArchetypeReveal.js` math, `MirrorDebrief.js`, `mrJourney.js`, `ReportsExport.js`, `trading-floor/page.js`, `stockValuationEngine.js`; the 13 prior audit/report/delta documents named in the brief (read for orientation and mined for claims).
**Sampled.** `pillar_configs.py` (scripted flag scan + texts of inert-flag options), `round_configs.py` (all option texts; R10), `healthcare_configs.py`, `round2_csrd.py`, `regulatory_sandbox.py`, `brsr_controller.py`, `pedagogical_engine.py` 441–560/643–733, `supply_chain_network.py` 331–505, `consequence_dna_api.py` 70–230/518–601, `journey_improvements.py`, `regional_reporting.py`, `market_dynamics.py`, `board_governance.py`, `negotiation.py`, `coordination_store.py`, `admin_ws.py`/`ws_fanout.py`, `ExecutiveCockpit.js` (belt, results overlay, gates, commit CTA, layout CSS), `StakeholderMapModal.js`, `DoubleMaterialityMatrix.js`, `PlayerRegistry.js`, `CreateCohortModal.js` (pacing, paradigm, seed), `SustainabilityBalancedScorecard.js`, `LeaderboardMatrix.js`.
**Not opened.** `validation_logic.py` body; `dynamic_cases.py`; side-track track logic beyond `brsr_ngrbc/track.py` M_R writers; `EBITDAWaterfall.js`, `BalanceSheetModal.js` beyond grep, `AnnualReport.js` beyond :41, `InvestmentMatrix.js` beyond the banner thresholds, advanced-climate module components beyond their fetch sites, `CohortComparison`/`PlatformAnalytics`; `load_tests/*.js` bodies; the .docx/.pptx manuals (only the S3 facilitator manual and release note were text-extracted for the runbook check); `docs/*.xlsx`.
**Not verifiable here — exact procedure for the owner.** (1) Production `/data/simulation_config.json` — F-07 inspect command. (2) Which branch Railway deploys — Railway → Service → Settings → Source; after deploy `curl https://<domain>/api/health | jq .build` (`main.py:648-650` reports `RAILWAY_GIT_BRANCH`/commit). (3) Volume actually mounted — Railway → Volumes; or from a service shell `grep " /data " /proc/mounts`; then DEPLOYMENT_CHECKLIST.md:46-48's create-facilitator → Redeploy → still-logs-in proof. (4) Postgres behaviour under 50 commits — `PG_PARITY=1 DATABASE_URL=… pytest tests/test_postgres_parity.py` and `k6 run load_tests/throughput_test.js` against a staging URL, never production during class. (5) Railway edge `X-Forwarded-For` (the limiter keys on the first XFF entry when the direct peer is a trusted proxy, `rate_limit.py:162-171`; if XFF were absent all clients share one bucket — still under 300/min at 50 users). (6) `LLM_API_KEY` set or not (RNG-5 CEO-interview fallback noise is unseeded only when unset and the interview is enabled). (7) Manual browser checks: 1366×768 sign-in → R1 allocation → commit CTA visible without horizontal scroll (CSS read: sticky `.actionBar` inside a scrolling `.centerConsole`, `ExecutiveCockpit.module.css:259-266, 3056-3060`; rails clamp ≤1280; no `innerWidth` check found for the CLAUDE.md "projector layer off <1280" claim); redeploy while a tab is on R1 then open the R2 matrix (no `ChunkLoadError` handling exists → ErrorBoundary "Something went wrong / Reload Page"); kill wifi 90 s mid-allocation and mid-commit; Timed cohort → briefing-screen chip vs header countdown; reset a player's password during R2 and submit the matrix (expected: React child error → ErrorBoundary, PLAUSIBLE).
**Harness scope statements.** `tests/test_treasury_waterfall.py` / `test_universal_math_engine.py` assert closure of `process_tick` alone with dividends/crisis/emergency off and never call `post_tick`/`run_new_engines`; their paradigm list includes `un_sdg` (rejected by `create_session`) and omits `brsr_ngrbc`. `audit_financial/*` reproduces FIN-02/FIN-07/inv6/inv7 failures on HEAD (it is a diagnostic, not a gate). `test_flag_taxonomy.py` is green while blind (BOM-skipped side-track configs; option-key names count as reads). `test_mr_single_arbiter.py` pins the finale with `{}` flags, so it cannot see F-15. `test_config_introspect.py` documents the endpoint's intended scope, which excludes clamps and stale-but-valid values. `test_admin_route_guards.py` counts F-11's inline check as tenanted. `scripts/monte_carlo_stress_test.py` runs `process_tick` only (no post-tick, no seed) and was not used for numbers.

---

## Appendix A — Model map (one line per engine; config keys grep-confirmed)

| Module · symbol | Quantity owned | Called by | Config it reads | State it writes | Fires |
|---|---|---|---|---|---|
| `engine.process_tick` → `_run_stochastic_layer` (:2592) | black swans, cannibalisation, contagion, macro/FX noise, DSO, inflation | `router.commit_turn:2811`, `dry_run`, tests | `difficulty_tier` flag → `black_swan_registry`, `BLACK_SWAN_MAX_EVENT_PROBABILITY` | `current_global.corporate_treasury` (pre-CSF), BU revenue/opex transients | every round |
| `engine._run_financial_layer` (:2781) | CSF pool, dividends clamp/ratchet, CapEx allowance + term loan + interest, pending projects, natural decay (:3096), tech debt, lock-in, fatigue (dead), contagion, brain-drain, NCD interest, debt service + floor #1, CAROIC | `process_tick` | `FINANCIAL_FREE_CSF_PCT`, `CSF_POOL_FLOOR`, `FINANCIAL_CAPEX_CAP_MULTIPLE`, `FINANCIAL_DEFAULT_LOAN_RATE` (masked by the session flag), `FINANCIAL_EMERGENCY_*`, `SIM_ROUNDS`, `FINANCIAL_TREASURY_FLOOR`, `FINANCIAL_INSOLVENCY_THRESHOLD`, `FINANCIAL_CORPORATE_TAX_RATE`, `NATURAL_DECAY_*`, `TALENT_*`, `BRAINDRAIN_*`, `NCD_*` | `ctx.new_treasury`, BU opex/revenue/NCD/reputation/SLO, `events.capex_loan_*`, waterfall | every round |
| `engine._run_operational_layer` (:3416) | ESG-WACC hook, CBAM (R3/R7, advanced_climate), carbon offsets, internal carbon fee, loss & damage | `process_tick` | `CBAM_*`, `FINANCIAL_SHADOW_CARBON_PRICE`, `ECONOMIC_CARBON_PRICE_*` | treasury, events | conditional |
| `engine._run_reporting_layer` (:3950) | greenwash check (:3970-4012), sentiment update (:4401-4455), distress/bailout, floor #2, waterfall finalise (:4163), regulatory ratchet (:4301, after finalise), supplier defection, momentum, forecast, valuation preview (:4762) | `process_tick` | `GREENWASH_*`, `REG_RATCHET_*`, `TURNAROUND_*` | treasury, `events.consequence_waterfall`, `events.valuation_preview` | every round |
| `engine._assemble_global_state` (:4513) | next `global_state`; `active_event_flags = events`; carries `ENGINE_STATE_KEYS` (8 sub-states; NOT `workforce_readiness`) | `process_tick` | — | `global_state` | every round |
| `engine.apply_natural_decay` (:776) | per-BU SLO/reputation tier | `:3096` (group-average ratio, per-BU capex) | `NATURAL_DECAY_FACTOR`, `_NO_DECAY_RATIO`, `_MID_RATIO`, `_GROWTH_RATIO`, `_MIN_ABS_CAPEX`, `_GROWTH_ABS_CAPEX`, `_MID_GROWTH` | BU `social_license_score`, `reputation_score` | every round |
| `round_logic.pre_tick` (:145) / `_pre_r4_contagion` (:213) | R4 severity 40/80/60 × map-accuracy 1.25 | `router:2769` | round configs | `crisis_severity_*` | R4 |
| `round_logic.post_tick` (:487) + `_POST_TICK_MAP` (:3242) | per-round option effects, `ledger_option_treasury`, R5 cyclone, green-fund offset, EU-AI-Act liability (every round), pulse revert (dead), R10 finale | `router:2901`, `admin_router:3487` (auto-commit; passes paradigm), `dry_run` | round/pillar configs, pathway `special_rules` | treasury, flags `rN_flags`, terminal keys | every round; finale R10 |
| `round_logic.run_new_engines` (:655) | order: biodiversity :695 → balance sheet :743 → board :769 → org politics :794 → supply chain :813 → NPC :843 → engagement :979 → agents :1006 → branching → sandbox → analytics; tipping caps :1039 | `router:2919` | pedagogical toggles, `get_difficulty_config` (covenant ratio) | `gs.balance_sheet`, `gs.corporate_treasury` (fines, agent costs, BS interest, surcharge), `gs.group_reputation`, BU SLO/opex, sub-states | every round |
| `balance_sheet.process_balance_sheet_tick` (:516) | statement, sweep + revolver interest (:509-514), depreciation, WC, provisions, loan mirror (:736, never fed), P&L, RE plug (:858), covenants + surcharge (:899) | `round_logic:743`, `router.get_balance_sheet:7293` (persists a tick on GET) | `CONSTRAINT_MIN_LIQUIDITY_RATIO`, `FINANCIAL_CORPORATE_TAX_RATE`, `FINANCIAL_WACC_LENDER_THRESHOLD`, `BS_*` | `bs`, `gs.corporate_treasury`, history | every round |
| `terminal_valuation.calculate_mr` (:106) | M_R raw/clamped + breakdown | `round_logic:2649` (finale), `:413` (BRSR), `engine:4764` (preview, raw flags), `consequence_dna_api:321`, `pedagogical_engine:454`, what-if | module constants (`MR_FLOOR 0.0`, `MR_CEILING 2.05`) | — | R10; preview every round |
| `terminal_valuation.calculate_dynamic_exit_multiple` / `calculate_equity_bridge` | Gordon multiple; equity, price/share ($1 floor) | `round_logic:2747/2771` | `TV_LONG_RUN_GROWTH`, `TV_EXIT_MULTIPLE_FLOOR/CEILING`, `TV_SHARES_OUTSTANDING`, `TV_IPO_PRICE` | — | R10 |
| `round_logic._post_r10_grand_finale` (:2293) | terminal EBITDA, carbon cost, M_R award, TV (:2750), equity bridge, two-axis profile/archetype, `mr_breakdown`, `hr_roi_report` | `post_tick` | pathway config | `active_event_flags[terminal_value, regenerative_multiple, …]`, `final_report_canonical` | R10 |
| `ending_pathways.calc_*_mr` | pathway deltas (ramped); climate exit-multiple haircut | `round_logic:2631-2641` | — | `extra[mr_*]` | R10 |
| `npc_stakeholders.process_npc_tick` | 4 NPCs: satisfaction, trust (F1), escalation, patience (F4), promises, fines | `round_logic:832-960` | `TRUST_*`, `NPC_SENTIMENT_BRIDGE_*`, `THRESHOLD_JITTER`, `PATIENCE_LIMIT`, `PROMISE_*` | `gs.group_reputation`, `gs.corporate_treasury` (fine, unseeded in prod), BU SLO if F2 ON, `gs.npc_stakeholders` | every round; ON by default; not displayed |
| `autonomous_agents` | 5 agents: tolerance, stage, grievance, triggered events | `round_logic:995-1035` | `THRESHOLD_JITTER`, `PATIENCE_LIMIT`, scar consts | treasury (−4/−8/−3%, −$3M), reputation (−8…−20), CoC (+0.02), BU SLO/opex/burnout | every round; ON; displayed in cockpit panel |
| `org_politics.process_org_politics_tick` | C-suite satisfaction ×3, political capital | `round_logic:794-810`; `router:6990` coalition-check (no caller) | — | `gs.org_politics` only | every round; display-only |
| `stakeholder_sentiment` | attitude scores, narratives | `engine:4403-4455` | — (module constants) | `events.sentiment_*` → flags | every round; display-only; rules never fed |
| `stakeholder_engagement` | promise ledger | `round_logic:979-993` | `ENGAGE_*` | treasury (cost), NPC trust | never (no UI sender) |
| `stakeholder_map` | catalogue, quiz scoring, salience | `engine:4404`, `round_logic:579`, `router:4698` | — | quiz results, accuracy | R1 quiz + migrations |
| `rng_util` | `event_rng`/`event_seed` (SHA of cohort seed + round + event), `derive_cohort_seed`, `ensure_cohort_seed` | engine, impact_engine, black_swan_registry, systemic_risk_engine, router, both stores | — | `flags.stochastic_seed` at creation | — |
| `black_swan_registry` | 13 events, tiers (`easy/standard/expert`), impacts | `engine:2601-2627`; `admin_router:9257` forced; `round_logic:719` | `BLACK_SWAN_MAX_EVENT_PROBABILITY` | treasury, reputation, CoC, BU bases (permanent), flags | R2–R10 |
| `systemic_risk_engine` | ESG WACC, SCT, employer brand, tipping, materiality shocks, cascades, foreshadowing | `engine:3454/4268`, `round_logic:866/886` | `NPC_SHOCK_BASE_PROB`, `NPC_SHOCK_MAX_PROB`; callers pass `COALITION_TIER_MIN`, `CONTAGION_SAT_NUDGE`, `EMPLOYER_BRAND_*` | `gs.cost_of_capital`, flags `fired_materiality_shocks`/`systemic_tipping_state`, BU opex/SLO/NCD via tipping | every round |
| `biodiversity_engine` | EHI, species risk, ESV, TNFD | `round_logic:683-704` | — | `gs.biodiversity_state`, treasury; `extra.tnfd_mr_bonus` (no reader) | every round |
| `impact_engine` | R5 cyclone/resilience/NCD projects; R9 friction/strike/retraining | `_POST_TICK_MAP[5]/[9]` | round `special_rules` | treasury, pending projects, flags, BU opex/SLO/gov, reputation | R5, R9 |
| `sdg_linkage_engine` | SDG alignment 0–100 | `router:3557` GET | — | — | request; display-only |
| `meadows_leverage` | LP distribution, effectiveness | `router:7042`, `consequence_dna_api:518/536`, `round_logic:1138` (event only) | — | events | request |
| `turnaround_engine` | post-R10 arc, amended M_R | `admin_router:12150-12235` (opt-in) | `TURNAROUND_*`, `FINANCIAL_SHADOW_CARBON_PRICE` | `gs.turnaround_*`, treasury, reputation | post-R10 |
| `pedagogical_engine` | toggles, content; analytics (uncalled) | `round_logic:674/1120/1130`, `router:1900`, admin | — | `events.peer_prompts/timer` | R5/R6 |
| `market_dynamics` | permit market, talent, share | none (zero importers) | — | — | never |
| `supply_chain_network` | visibility, risk, scope3, disruption, audit | `round_logic:815-827`; `router:6978` audit (no UI caller) | — | `gs.supply_chain`; treasury on audit | every round; display-only |
| `tcfd_scenarios` | 3 scenarios | `router:6880` GET | — | — | request; display-only |
| `consequence_dna_api` | Sankey, impact score, DNA M_R projection | `router:3062/3515` | — | — | request (live at round 1, SEAM-11); R10 snapshot |
| `journey_improvements`, `round_recap_engine`, `regional_reporting`, `branching_engine` | content/labels; recaps (uncalled); traffic-light report; archetype branch modifiers (unconsumed) | `admin_teleprompter`, `admin_router:13015`, `round_logic:1098` | — | `_session_journey_responses` (process memory) | display |
| `config.py` chain | every tunable | all | image JSON → volume JSON (wins) → Excel upload (rewrites) → hot reload (5 modules) → god-mode `overrun_*` | — | import; hot reload |

## Appendix B — Claims ledger (prior claims, reproduced or not)

| # | Claim | Source | Reproduced? | Evidence |
|---|---|---|---|---|
| 1 | R6 EU AI Act deferred charge registered only at `_POST_TICK_MAP[6]`; R7+ consequence can never fire | AUDIT_ENGINES_DEEP_2026-08-31 (named lead) | **REFUTED** | `round_logic.py:554-556` calls `_apply_eu_ai_act_liability` every round; probe R7 −$3M/+5 gov, R8–R10 −$1M |
| 2 | Two arbiters for M_R (inline finale vs `calculate_mr`), inline path unclamped | same | CLOSED at the finale; projection-side half persists | `round_logic.py:2649`; 52 tests; F-15 |
| 3 | Money-conservation gap up to ~$656M/run | same :47 | NOT REPRODUCED (superseded) | pre-fix ratchet ceiling in `test_treasury_waterfall.py:15-20`; residual now 0.00 inside `process_tick`; production round moves outside the ledger by −$5.7M…−$20.1M (FIN-06) |
| 4 | A large share of declared flags inert | same | REPRODUCED with numbers | 381 set / 187 never read; 31 core flags with no taxonomy ruling; 46 read-never-set (`flag/flag_sweep.out`) |
| 5 | High-stakes stochastic draws bypass the fairness seed | same | PARTLY — engine draws seeded; regulator fine unseeded in production | F-04; draw-site table in `rng/findings.md` §C |
| 6 | Transient OPEX surcharges leaked permanently (B-2) — fixed 5cc0c2e | commit | REPRODUCED fixed | `fin/surcharge_transient_probe.py` diff 0.00 |
| 7 | Published M_R ceiling 2.020 unreachable (B-4) — fixed b1d0dd4 | commit | REPRODUCED fixed for the function; in-game only in `multi_toggles` (`hr_invested_r*` written only under `pillar_flags`) | `val/probe_mr_bounds.py` 1.93/2.02/1.98 |
| 8 | Natural-decay tiers now 0.10/0.25/0.50 + $500k | REPORT_launch_readiness :751-753; `engine.py:794-795`; `config_excel.py:200-203`; `config.py:636` | STALE — shipped 0.15/0.20/0.30 + $100k (278365d) | runtime print; `simulation_config.json:255-260` |
| 9 | Golden traces rebaselined against shipped values | 278365d | YES — byte-identical to pre-item-4 | `git diff --stat b1d0dd4 HEAD -- tests/golden/*` empty; 20 tests pass |
| 10 | "no_decay_ratio is structurally INERT given the $100k floor" | `config.py:589-592` | FALSE under engine semantics | F-17 (`soc/p2.log`) |
| 11 | Tier populations 70.1/0.0/3.7/26.1% over 241 decisions | `config.py:585-586`, DELTA docs | NOT REPRODUCIBLE (decisions not in repo) and measured on the wrong quantity | F-17 |
| 12 | F-08 stakeholder fatigue "acts on each BU's reputation gain" | MODEL_CARD.md:152-153; CHANGES:405-408 | FALSE — never fires | `soc/p5.log` 0/360 |
| 13 | "The cockpit re-opens the change-password modal on that code" | CHANGES_launch_blockers:147-149 | FALSE | F-01 |
| 14 | Volume config wins over repo copy; three poison values clamped with a warning | CHANGES:537-545 | YES | `runtime_paths.py:273-275`; probe WARNING |
| 15 | `regulatory_ratchet.baseline` 10.0 not clamped and would govern | CHANGES:592-594; REPORT:153-155 | YES | F-07 probe |
| 16 | Production volume still carries CBAM 100,000 | CHANGES:600-601 | NOT VERIFIED | F-07 settling command |
| 17 | `/config/live` reports `in_sync` on a stale copy; 16 low `inert_tunable` on a pristine one | CHANGES:598-606 | YES | `cfg/probe_config_live.py` |
| 18 | Excel endpoint rebuilds config from a "199-row sheet" | d3bbf09 message | Rebuild YES; 191 rows | openpyxl count |
| 19 | simulation-reference / engine-tunables / scenario-presets unauth — Do-Before-Class blocker with fix | AUDIT_Classroom50 §F02 | STILL OPEN | F-10 |
| 20 | `esg_profile_weights` publicly readable | Classroom50; COMPREHENSIVE_AUDIT | FIXED | `global-settings` → null unless facilitator/finished |
| 21 | X-Player-Id forgeable header replaced by signed token everywhere (F-22) | Independent_Review; CHANGES | PARTLY — `admin_analytics` missed | F-11 |
| 22 | "DEEP-8 RESOLVED — eight ledger terms explain every dollar, every round" | AUDIT_ENGINES_DEEP addendum | PARTLY — true inside `process_tick` with dividends/crisis/emergency off; false for the production round | FIN-06 |
| 23 | "CapEx … the balance sheet shows the debt" (F-05) | `engine.py:2926-2936`; CHANGES:362-375 | amortisation YES; statement NO | F-12 |
| 24 | "The waterfall shows EXACTLY how treasury moved" | `admin_teleprompter.py:1616-1620` | NO — frozen before the last entries; no UI renders it | FIN-06 |
| 25 | Glossary: "principal never debits treasury" | `admin_analytics.py:865` | NO — `engine.py:2941-2974` repays and debits | FIN-14 |
| 26 | BALANCE_REPORT_BASELINE.md numbers | scripts/balance_report.py | YES — byte-identical at HEAD | `fin/balance_report_HEAD.clean.md` |
| 27 | "No dead levers: every option moves TV ≥0.5%" | BALANCE_REPORT §3 | not contradicted for options; component dead levers exist | VAL-05, VAL-08 |
| 28 | "workforce_bonus 0/11 — require specific paths" | BALANCE_REPORT §2 | NO — structural (`workforce_readiness` resets to 50 every round) | VAL-05 |
| 29 | BRSR finale "verified end to end (422 → 201)" | REPORT ITEM 5 | creation YES; end-to-end NO | VAL-06 |
| 30 | Preview "is the same calculate_mr projection the finale uses" | `engine.py:4750-4756` | NO | F-15 |
| 31 | "Every stochastic event … identically for all teams" | `rng_util.py:4-6` | NO (regulator fine) | F-04 |
| 32 | Facilitator `rng_seed` "stamps stochastic_seed onto the live state" | `admin_router.py:1176-1178` | NO — both stores revert the flag | RNG-2 |
| 33 | `replay.py` reproduces a session | `replay.py` | PARTIAL by construction (1569 diffs / 9 rounds) | RNG-4 |
| 34 | Draft "restored (autosave/save-decisions)" | UX_VERIFICATION_CHECKLIST.md:33-37 | conditional (30 s idle, option chosen) | FLOW-05 |
| 35 | Wizard "Manual — Facilitator unlocks each round" | `CreateCohortModal.js:2267` | NO | F-18 |
| 36 | "A new random password will be generated" | `PlayerRegistry.js:264` | NO — deterministic `ID@123` | FLOW-07 |
| 37 | Volume durability verifiable via `durable=True` / `durable_storage` | DEPLOYMENT_CHECKLIST.md:39-45 | REFUTED — true with no volume mounted | OPS-3 |
| 38 | Health endpoint reports config-load state | Facilitator Manual S3 Ed2 (extract l.475) | REFUTED — that is `/api/admin/config/live`, super-admin only | OPS-7 |
| 39 | Cohort-wide rollback with the ROLL BACK phrase | `sidebarConfig.js:163` | REFUTED — button permanently disabled for shells; API partial + 400 | OPS-2 |
| 40 | Uploaded config "lives in the image, reverts on redeploy" | `SimulationSwitchboard.js:995`; `config_introspect.py:168`; `ConfigLiveStatus.js:13` | NO — JSON persists on the volume (`admin_router.py:9152`) | CFG-09 |
| 41 | "R6 is the earliest round a trigger can fire" | `admin_teleprompter.py:459` | FALSE — R4 under maximal breach | `soc/p4.log` |
| 42 | Greenwash "8-point SLO penalty", decay "2%/round ×0.98" | `TechnicalGlossary.js:194-197, 240-242` | FALSE — 15/7.5; ×0.96 with $100k + average-ratio gate | SOC-8 |
| 43 | "Classroom (Easy) … forgiving penalties" preset | `admin_router.py:10686-10690` | NO for the financial tier table (`foundation` → `standard`) | FIN-08 |

Full per-cluster ledgers (≈120 rows) are in the companion archive.

## Appendix C — Probe scripts (all in the companion archive under `audit_scratch/`; run with `source audit_scratch/env.sh; cd backend; timeout 170 python3 <script>` — `env.sh` sets `USE_MEMORY_DB=true`, `DEBUG=true`, a scratch `MURESSONS_DATA_DIR`, the test-only `MASTER_PASSWORD`/`PROJECT_ADMIN_PASSWORD`, `PYTHONPATH=backend`, `PYTHONDONTWRITEBYTECODE=1`; **never point `MURESSONS_DATA_DIR` at a real data dir**; `cfg/probe_hot_reload.py` and `acc/probe_admin_reach.py` write into the repo — do not re-run them on a working tree you care about)

- **lead/** `routes_dump.py` (walks `main.app.routes` incl. lazy included routers → `routes.json`, 367 routes with Depends), `unauth_sweep.py` (no-credential sweep → `unauth_sweep.json`).
- **fin/** `full_run_probe.py` (HTTP-driven 10-round runs, all valid paradigms × tiers; outputs `full_run_capex3m.json`, `full_run_capex1m.json`), `recon_full.py` (waterfall closure, A=L+E, RE bridge, cash articulation, BS loan vs flag loan), `round_detail.py`, `stage_trace.py` (treasury delta per pipeline stage), `line_trace.py` (`sys.settrace` attribution of every treasury write outside `engine.py`), `bs_loan_probe.py` (F-12), `bs_get_probe.py` (FIN-04), `surcharge_transient_probe.py` (B-2), `post_csf_trace*.py` (FIN-10), `capex_grid.py` (E6 lever grid via `dry_run`), `emergency_credit_probe.py` (FIN-05), `dividend_clamp_probe.py` (FIN-12), `law_all_paradigms.py` (conservation on all valid paradigms), `run_trace_local.py`/`recon_local.py`/`re_bridge_local.py` (path-fixed copies of `audit_financial/*`), `run_*.sh` runners; `balance_report_HEAD.md` (byte-identical to BALANCE_REPORT_BASELINE.md).
- **val/** `probe_mr_bounds.py` (config TV keys, floor/ceiling, max/min states), `probe_fullgame.py <paradigm> <10 letters>` (per-round preview vs finale-style M_R, R10 keys, surfaces), `probe_fullgame_pillar.py`, `probe_readiness.py` (VAL-05), `probe_brsr_routing.py` (VAL-06), `probe_leaderboard.py` (F-14), `probe_synergy_wipe.py` (VAL-07), `probe_solvency_axes.py` (VAL-09), `probe_preview_vs_finale.py` (F-15, 384 states).
- **rng/** `harness.py` (TestClient driver: god-mode login, cohort, players, gates, canonical→display option translation, commit, summarise — reused by four clusters), `probe_diverge.py` (F-04, 3 identical teams R1–R4 → `probe_diverge.json`), `probe_full_identical.py` (control with the NPC draw stubbed), `probe_fix_verify.py`, `probe_perfect_map.py`, `probe_seed_*.py` (RNG-2), `probe_two_players.py`, `mc_luck.py`/`mc_analyze.py`/`mc_results.jsonl` (F-09 Monte-Carlo), `smoke.py`.
- **flag/** `flag_sweep.py` (declared/set/read sweep → `flag_sweep.out`/`.json`), `probe_eu_ai_act.py`, `probe_deferred.py` (F-05), `probe_phantom_proxy.py` (FLAG-2), `probe_sentiment_flags.py` (FLAG-3), `probe_endpoints_flagdeps.py` (FLAG-4/7), `probe_dna_status.py`.
- **soc/** `p1_shipped_values_grid.py` (runtime constants + decay grid), `p2_avg_ratio_tiering.py` (F-17), `p3_ten_round_table.py`, `p4_agent_earliest_trigger.py`, `p5_fatigue_dead.py` (SOC-3), `p6_prod_defaults_npc.py`, `p7_patience_clock_fines.py`, `p8b/p8c/p8d_*.py` (fines vs seed vs strategy), `p9_greenwash_not_betrayal.py`, `p10_patience_cost.py` (F-16), `p11_tier0_reachability.py`, `p12_greenwash_message.py` (SOC-7), `run_golden.sh`.
- **imp/** `imp_state.py`, `probe_bio.py` (F-19a), `probe_tcfd.py` (F-19b), `probe_meadows.py` (F-19c), `probe_sdg.py` (F-19d), `probe_tipping.py` (IMP-10), `probe_fullgame.py` + `run_fullgame.sh` (production call chain, 3 bots × 2 paradigms), `probe_blackswan_treasury.py`, `probe_blackswan_persist.py` (IMP-05), `probe_toggles_dead.py` (IMP-06), `probe_turnaround.py`, `probe_regional.py` (IMP-12), `probe_impact_r9.py` (IMP-15), `probe_market.py`, `run_tests.sh`.
- **cfg/** `config_diff.py` (config.py defaults vs JSON → `config_diff.out`), `from_config_imports.py` (AST scan of `from config import` sites vs the reload chain), `probe_config_live.py` (clean vs stale scratch volume), `probe_e2e_cbam_tax.py` (F-07; tax and CBAM back-outs), `probe_excel_malformed.py` (9 corruption cases), `probe_hot_reload.py` (touches the repo xlsx), `probe_boot_env.py` (SEC-6 exit), `provenance_diff.py`.
- **ops/** `probe_commit_race.py`, `probe_timed_autocommit.py` (OPS-1), `probe_force_advance_race.py`, `probe_restart_phase1/2.py`, `probe_recovery.py`, `probe_end_date.py`, `probe_poll_cost*.py`, `probe_idempotency.py`, `probe_health.py`.
- **flow/** `probe_pwchange.py` (F-01), `probe_flow.py` (run-ahead, double/stale submit, client-only gate, over-cap, draft round-trip, manual pacing), `probe_advance.py` (Force Advance), `probe_pacing_scope.py`, `probe_wizard_pacing.py` (F-18); `jest/pwmodal.test.js` (renders `app/page.js`; command in `flow/findings.md` §I).
- **seam/** `play.py` (cohort seam-A, saves pre/commit/post per round), `pull.py` (every surface → `surfaces_A.json`), `compare.py` (disagreement tables), `pred_probe.py` (SEAM-09).
- **acc/** `sweep_noauth.py`, `dump_public_bodies.py` (→ `bodies/`), `sess_routes_static.py` (AST guard scan), `acc_setup.py`, `probe_isolation.py`, `probe_admin_reach.py` (279 admin routes × 4 actors; caused the pillar write), `probe_analytics_player.py` (F-11), `probe_crosscohort.py` (ACC-4), `probe_ws.py`, `probe_tokens.py`, `probe_peer.py`, `probe_roundcfg.py`, `debug_false_check.py`.

## Appendix D — Register of the other cluster findings (not in the top 20; one line each; full text in the archive)

**P1 (cluster severity).** FLAG-2 `multi_toggles` persists the legacy proxy option's flags — an omitted pillar area silently sets `insurance_only`/`electronics_blindspot`/`early_decarboniser` (`router.py:2646-2649`, `pillar_configs.py:1706-1712`, `round_logic.py:3344-3349`). FLAG-3 the 58-flag sentiment rule map never receives a decision flag (`engine.py:4424-4427`). FLAG-4 flag-dependencies / DNA "active" status tests dict keys on list-held flags (`terminal_valuation.py:690`). FLAG-6 R5 resilience protects nothing after R5; R10 `climate_resilience` constant 0.5. IMP-04 R6/R7/R8 journey panels promise effects never applied and PATCH a super-admin route as a player. IMP-05 black-swan revenue/OPEX % are permanent base erosion; headline hit understated 4–5×. IMP-06 god-mode "Systemic Risk Controls" toggles have no reader. IMP-07 materiality shock: blocking critical modal, no state effect. IMP-09 resilience factor inert after R5. SEAM-08 history row 10 overwritten → R10 lost from every history consumer, R9 deltas wrong in admin analytics. SEAM-09 predicted-vs-actual pairs round r's prediction with round r−1's outcome. SEAM-11 live Consequence-DNA built at round 1. OPS-1 timed-pacing auto-commit bypasses the commit path, wipes 26 flags incl. the seed, writes an R11 row (API-only; the shipped UI does not reach it). OPS-3 `durable_storage: true` even with no volume mounted. FIN-10 brain-drain OPEX ×2.975/round on healthcare/software; treasury floor bypassed by post-tick writers. VAL-06 `brsr_ngrbc` never reaches the BRSR finale from `commit_turn`. RNG-2 facilitator `rng_seed` never reaches live state (fairness unaffected). ACC-4 any facilitator can reset any cohort's player password.
**P2.** FIN-04 pre-commit GET `/balance-sheet` ticks and persists R1 twice. FIN-05 emergency credit phantom ($140k/round interest, $1M never credited; UI threshold $5M vs server $25M). FIN-06 conservation law covers `process_tick` only; waterfall frozen before the last entries; no UI renders it. FIN-07 RE plug with `balance_sheet_balanced` hard-coded True. FIN-08 difficulty vocabulary mismatch (`foundation` → `standard`); 4 dead tier keys. FIN-09 regulator fine uncapped by tier, outside the P&L. FIN-15 CapEx share 5–60% non-monotonic; seed noise exceeds the lever. VAL-05 `workforce_readiness` resets to 50 every round (`ENGINE_STATE_KEYS`); +0.10 Workforce Excellence unreachable. VAL-07 R10 Divest resets synergy to 1.0 and earns the +0.15 premium. VAL-08 climate +0.15 dead (`baseline_ci` never passed). VAL-09 two solvency definitions. VAL-10 −0.40 instability discount fires in ~all runs. SOC-3 fatigue dead. SOC-4 greenwash scandal not a betrayal flag. SOC-5 org_politics display-only, presented as consequential. SOC-9 volume `slo_ramp` may be stale (not patched by the 7-key patcher; settle with F-07 inspect output `slo_ramp`). FLAG-5 adaptive crisis severity computed and announced, never applied. FLAG-7 advanced-climate module overlays POST to seven 404 endpoints. FLAG-8 three boolean-key reads of list-held flags (`ethical_ai_overhaul` −5pp, `hard_engineering` pulse revert, `carbon_deferred` futures market). FLAG-9 `greenwashing_detected` read in five places, written nowhere. FLAG-10 seven text/number promises the model contradicts. IMP-10 social tipping SLO direction inverted. IMP-11 black-swan transparency modifier reads a never-written key. IMP-12 regional ESG report Scope 1+2 = 0 GREEN. IMP-13 TCFD arithmetic (squared demand shift, ZeroDivision at revenue 0). IMP-14 turnaround placeholder calibration. IMP-16 tipping penalties compound per round vs documented one-off. CFG-04 shipped xlsx stale (imitation 0.1), importer drops 17 keys. CFG-05 hot reload leaves `router.py`'s captured `DEFAULT_IMITATION_DECAY_RATE` stale. CFG-06 importer silent on negatives/out-of-range/bool typos. CFG-07 SETUP_STEPS.md `JWT_SECRET` ≠ `.env.railway` (plaintext secrets in a gitignored doc). CFG-03 `/config/live` blind to clamps, `healthy:false` on a pristine install. CFG-02 no runbook step refreshes the volume config. OPS-2 cohort-wide rollback unreachable from the UI; API rolls children back then returns 400. OPS-4 `/api/health` never touches the database. OPS-5 stakeholder-map resubmission re-applies penalty/bonus. RNG-4 `replay.py` PARTIAL. RNG-5 CEO-interview fallback unseeded when `LLM_API_KEY` unset. SEAM-10 board-pressure −3 exists only in the commit response. SEAM-12 "peer benchmarking" percentiles span every session on the server (55–61 players for a 3-team cohort). SEAM-13 grading CSV exports the proxy TV; trading floor reads a `price_per_share` never sent. SEAM-14 projected surfaces hard-code "$" and bypass the cohort currency rate. SEAM-15 CI unweighted mean vs revenue-weighted emissions; benchmark percentile from a static table. SEAM-16 two WACCs (cockpit 9.45% vs exit-multiple 7.45%). SEAM-18 negotiation `or 50` (PLAUSIBLE). FLOW-05 autosave only after 30 s idle with an option chosen. FLOW-06 "Last Round Summary" empty; reload skips the results screen. FLOW-07 reset-password says "random", resets to `ID@123` and re-arms the gate. ACC-6 pillar-overrides endpoints write into `backend/db/` inside the image (lost on redeploy) and any facilitator edits a platform-global config.
**P3.** FIN-11 `FINANCIAL_DEFAULT_LOAN_RATE` masked by the session flag; CSF pool constants captured at import. FIN-12 clamped dividends recorded at the requested amount. FIN-13 insolvency consequences narrative-only; R10 loan draw interest-free. FIN-14 glossary contradicts F-05. VAL-11 hygiene bundle (HR uplift multiple, dead healthcare 14×, `/final-report.terminal_valuation` always null, stale `max_mr 1.98`, `max_achievable_mr 1.93` for pillar teams, hostile-takeover cap after the arbiter, front-page band ignores solvency, BRSR dividend float never collected, pillar `waste_to_energy` lacks the synergy boost). SOC-6 F5 engagement ON with no player entry point. SOC-7 greenwash messages state the wrong bar. SOC-8 four stale decay descriptions. FLAG-11 `test_flag_taxonomy.py` green while blind (BOM files skipped; `frontend/src` path does not exist). FLAG-12 `detect_betrayal` can never fire. IMP-15 pillar-mode R9 clawback message false. IMP-17 dead/mis-labelled instruments (uncalled pedagogical analytics, empty MidGameCheckpoint, CalibrationReport route missing, `tnfd_mr_bonus` unread, deforestation "hectares" rendered as %, `market_dynamics` unimported, duplicate DNA key). CFG-08 3 inert JSON keys and 16 inert god-mode tunables; `overrun_risk.*` dead. CFG-09 in-code drift (config.py:637 "500,000"; :489-492 seed comment; provenance names 5 non-existent keys and a missing doc). OPS-6 player password reset does not invalidate the old token. OPS-7 runbook drift (init.sql trigger vs boot copy; manual claims a health field that does not exist; inert cohort `end_date`). RNG-6 enforcement message amount ≠ deducted. RNG-7 "seeded per cohort" jitter is a constant. RNG-8 unseeded draws on API-only/off-by-default paths. SEAM-17 belt deltas compare against the current round's own snapshot at rest. FLOW-08 pre-2026-08-02 bundles double-commit after a lost response. FLOW-09 R1/R2 gates client-only (skipping is penalised, not rewarded). FLOW-10 facilitator freeze reported as "server busy". FLOW-11 RunBar "Advance" is the non-idempotent bare unlock. ACC-5 projector tab has no JWT refresh — stale after 8 h.

---

## Live-session runbook (print this page)

**Pre-flight — the day before (owner)**
1. Branch pushed; Railway → Settings → Source shows it; `GET /api/health` → `.build` commit ≥ `d3bbf09`. Volume mounted at `/data`.
2. Run the F-07 inspect one-liner. Expect `cbam 100 imit 0.05 ratchet 20.0 synergy 0.06 ncd 5000 1000 1000`. Otherwise patch, Restart, re-inspect. Deploy log: no `[CONFIG] WARNING`.
3. Logged out, open `https://<host>/api/admin/teleprompter` — expect 401 (if item 6 of the do-before-class list was done). If it returns JSON, the answer key is public.
4. Create the cohort. Then Round Timeline → Round Pacing → **Manual** → Apply (the wizard's pacing is ignored). Cohort settings: turn OFF biodiversity, TCFD, mirror debrief, M_R ladder, rewind ribbon, R6/R7/R8 journey panels; keep Trading Floor OFF; if F-04 is unpatched turn OFF NPC stakeholders.
5. Three test players: sign in → (if F-01 unpatched: Forgot your password? → `ID@123` → new password) → R1 map → commit → R2 matrix. Admin leaderboard shows all three. Undo Round on one of them → it re-commits.
6. Print this page. Slide 1 carries the password recipe (if F-01 unpatched) and "do not reload after committing".

**Pre-flight — 30 minutes before (facilitator)**
- Facilitator console open in one tab (it refreshes its own login); projector tab opened fresh (it does not — reopen it after 8 h).
- RunBar health dot green means only that the web server answers; if commits start failing while it is green, the database is the suspect — Railway → Restart the service.

**The three most likely failures**

| Symptom on a participant's screen | Cause | Recovery (facilitator, no developer) |
|---|---|---|
| R1 stakeholder map: "Server returned incomplete data. Please retry." / commit chime plays but nothing happens / matrix will not submit | First-login password gate (F-01): every write is refused until the initial password is changed, and the change screen never appears after login | Tell them: **Log out (🚪, click twice) → "Forgot your password?" → team ID, current password `ID@123`, new password (≥8 chars) → sign in.** 2 minutes. If you reset someone's password from the Player Registry, they must do this again. |
| "Waiting for Other Teams 7/8 committed" for minutes; or one table is two rounds ahead of the room; or "Unlock failed — target_round must be between 1 and 10" | Free-mode barrier is client-only and the wizard's pacing was never applied (F-18) | Round Timeline → Round Pacing → click **Manual** → Apply; then unlock each round from the same panel (Unlock, with the target round shown). In free mode, **Force Advance Now** in the Free Play panel commits absentees from their draft or with Option B/$1 and tells them so. Never use "Timed" with an interval. |
| The projector disagrees with the team's screen (treasury, reputation, decision label, terminal value) | Debrief surfaces recompute or mis-attribute (F-02/03/06/08/14) | Trust, in this order: the **admin Leaderboard** columns treasury / reputation / avg SLO / M_R; each team's **scorecard** M_R, archetype and terminal value; admin analytics `decision_impact` for the choice per round. Do **not** read aloud: Round Debrief "Decision Made", Debrief Narrative reputation / turning points / predicted-vs-actual, Front Page "Closing treasury" or "Road Not Taken", Trading-Floor / God-Mode "Terminal Value", the balance-sheet cash line, the share price, the TCFD tab, the Meadows card, the SDG radar. |

**Other quick fixes**
- Team stuck on a round after a mistake → Live Classroom → Undo Round (select the *player* session; needs a lead-facilitator login) → team re-commits. Cohort-wide rollback does not work from the UI.
- Team's numbers went blank / seed-dependent oddities after a "Timed" expiry → Undo Round for that team; switch to Manual.
- "Server busy" toast on every commit while nobody is committing → the cohort is frozen in god-mode settings; un-freeze.
- Reload before reading the final reveal (the live one shows round-10-entry numbers).
- Day 2: everyone signs in again with their personal password; nothing else is needed. Do not reset passwords the night before.

**Escalation.** Facilitator → owner (developer) for: any `500` visible to more than one table, a Railway restart that does not bring commits back within 3 minutes, or a config warning in the deploy log. Owner has: Railway Restart / Redeploy of the previous deployment (schema migrations are additive, an older image ignores new columns — not tested), the F-07 one-liners, `railway logs`. Nobody else needs credentials.

**Projection rules (one line).** Rank and debrief on M_R, reputation and SLO from the admin leaderboard; show treasury only beside the cohort's black-swan list; say the NCD line is an index; say the term loan lives in the finale's net debt, not on the round statements.

*Audit lead: Claude (Cowork), 2026-09-04, on `d3bbf09`. Eleven cluster reports, ~120 probe scripts and all logs are in `Claude outputs/audit_2026-09-04_probes_and_cluster_reports.tar.gz`.*
